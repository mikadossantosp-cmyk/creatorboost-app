# CLAUDE.md — CreatorBoostX

> Persistenter Kontext für Claude-Sessions. **Sprache: immer Deutsch** (du-Form, konkret, kein Fluff).
> Diese Datei ist die Single Source of Truth für „wie wir hier arbeiten". Bei Widerspruch zur Realität → Code gewinnt, Datei korrigieren.

---

## 1. Produkt

**CreatorBoostX** (auch „CreatorX" / „CreatorBoost") — Web-/PWA-App für **Instagram-Creator-Wachstum** über gegenseitiges Engagement (Likes/Kommentare auf echte Insta-Reels) mit XP-, Diamanten-, Missionen- und Community-System.

Zielgruppe: deutschsprachige Instagram-Creator. UI komplett auf Deutsch, mobile-first.

## 2. Architektur (verifiziert)

- **Stack:** Node.js **20.x**, **kein** Frontend-Framework. `bot.js` ist ein **monolithischer HTTP-Server (~11.500 Zeilen)** mit **serverseitig gerendertem HTML** (Template-Strings) + handgeschriebenem CSS.
- **Einzige Prod-Dependency:** `web-push`.
- **Entry (Dev):** `node bot-loader.js` (wendet Laufzeit-Patches an) · **Entry (Prod/Docker):** `node bot.js` (Patches via `node patch-bot.js` eingebacken).
- **Port:** 3000 (`PORT` überschreibbar).
- **Standalone:** Der frühere „MainBot" ist **raus** — `MAINBOT_URL` entfernt (Phase 3b2). Die App läuft eigenständig.
- **Datenhaltung:** **keine echte DB**, JSON-Dateien auf Disk via `datastore.js` (`daten.json` wird zur Laufzeit erzeugt, ist gitignored). Sessions/Push-Subs ebenfalls als JSON.
- **Helper-AI:** Google **Gemini** (`gemini-2.5-flash-lite`, Fallback `gemini-2.0-flash-lite`). In-App-Assistent „CreatorBoost". System-Prompt: `HELPER_SYSTEM_PROMPT` ab `bot.js:529`. Aktiv nur mit `GEMINI_API_KEY`, sonst Keyword/Admin-Fallback.

### Pflicht-Env-Variablen (sonst FATAL beim Start)
| Variable | Zweck |
|---|---|
| `BRIDGE_SECRET` | Auth-Secret |
| `VAPID_PUBLIC` / `VAPID_PRIVATE` | Web-Push VAPID-Keys |

VAPID lokal erzeugen: `node -e "const wp=require('web-push');const k=wp.generateVAPIDKeys();console.log('VAPID_PUBLIC='+k.publicKey);console.log('VAPID_PRIVATE='+k.privateKey)"`

## 3. Design-System (existiert bereits)

- **Design-Tokens** in `:root` ab `bot.js:1739`: Farben, `--radius*`, `--font`/`--font-display`, **Type-Scale `--fs-xs…--fs-3xl`**, **Spacing-Skala `--space-0…--space-16` (4px-Basis)**, `--lh-*`, `--fw-*`, `--shadow*`, `--ring`. Themes `[data-theme=light|dark]`.
- **Komponenten-Kit `ui-*`** (~`bot.js:1803–1823`): `.ui-btn` (+`--secondary/--ghost/--sm`), `.ui-card`, `.ui-input`/`.ui-field`/`.ui-label`/`.ui-hint`, `.ui-modal*`, `.ui-empty*`.
- **Live-Styleguide:** Route **`/styleguide`** (`bot.js:7415`).
- Reduced-Motion ist gehandhabt (`bot.js:1791` no-preference, `1799` reduce).
- Eingeführt in Commit `7d762e6` „UI-Fundament v1".

> **Regel:** Neue UI **immer** auf Tokens + `ui-*`-Kit bauen. **Keine** neuen Inline-Styles, **keine** hardcoded `font-size`/Abstände.

## 4. Bekannte Technische Schulden (Adoption-Lücke)

Foundation existiert, ist aber nicht flächendeckend genutzt:
- **~2.661 Inline-`style=`** Attribute in `bot.js`.
- **~1.400 hardcoded `font-size:NNpx`** (umgehen die Type-Scale).
- **5 fragmentierte Farb-Paletten:** Gold-Seiten (`bot.js:5739, 5862, 6216, 6302`), Dashboard mit eigenem Namespace `--dgold/--dink` (`15278`), Landing (`8921`). → Konsistenz-Risiko.
- Nur **12 `@media`**-Queries (mobile-first, aber dünne Breakpoint-Abdeckung).

**Strategie:** Screen-für-Screen-Migration auf das Kit (Reihenfolge nach Traffic). Pro Screen: migrieren → `node --check` → committen → pushen.

## 5. Arbeitsmodus (Session-Prinzipien)

Prioritäten **immer in dieser Reihenfolge**:
1. **Stabilität** 2. UX 3. Geschwindigkeit 4. Retention 5. Engagement 6. Skalierbarkeit 7. Monetarisierung 8. Code-Eleganz

- **Anti-Halluzination:** keine erfundenen APIs/Framework-Features; Unsicherheit benennen; erst Code/History lesen, dann behaupten.
- **Production-ready only**, modular, mobile-first, secure by default, geringe Komplexität.
- UI-Qualitätslatte: Stripe / Linear / Notion / Vercel / Apple — minimalistisch-premium, subtile Animationen, kein Template-Look.
- **Ehrliche Grenzen von Claude (kein Floskel-Theater):** kann das eigene Modell **nicht** selbst umschalten (= `/model`-Einstellung des Users); unterdrückt **keine** echten Risiko-Hinweise.

## 6. Konventionen & Verifikation

- **Keine Tests, kein Runtime-Linter-Gate.** Verifikation: **`node --check bot.js`** (Syntax). Optional Server starten + Endpoints curlen + Seite im Browser prüfen.
- **Dokumentations-Pflicht (User-Wunsch, dauerhaft): JEDE neue Änderung/Feature SOFORT hier in `CLAUDE.md` festhalten** (Abschnitt 7 Log + ggf. Env/Abschnitt 2), damit immer auffindbar ist, was gemacht wurde. Gilt vor jedem Push.
- **Branch/Push (User-Wunsch, dauerhaft):** **Immer auf `main` pushen** — committen, dann als **Fast-Forward** auf `main` (`git push origin <feature>:main`). **Keine PRs.** Aktueller Arbeits-Branch: `claude/chat-performance-issue-ZtaCk`.
- Commit-Messages: kurz, beschreibend, deutsch.
- `bot.js` ist riesig → Änderungen gezielt, additiv, kollisionssicher (es gibt bereits `.btn`, `.card`, `.post*` etc. — Klassennamen prüfen, nicht überschreiben).

## 7. Entscheidungs-Log (chronologisch)

- **Prompt 1 „CreatorBoostX Core Operating System":** als **Session-Modus** übernommen (kein File), Stil prägt die Arbeit.
- **Prompt 2 „Ultimate UI/UX Transformation":** Audit ergab → Foundation existiert schon; echte Arbeit = **Adoption/Migration**, nicht Redesign. Stack ist SSR-HTML, **nicht** React/Tailwind (kein Framework-Rewrite ohne separate Entscheidung).
- **Feed-Migration:** Post-Karte (`bot.js:1883–1901`) — doppelte `.post-time`-Regel entfernt, Abstände auf `--space`-Skala, `font-size` auf `--fs`-Tokens. Tab-Trigger auf `--fs-base`. Feed war bereits ~90 % klassenbasiert; Rest-Inline-Styles sind legitim dynamisch → abgeschlossen.
- **Dashboard an App-Stil angeglichen** (`/dashboard`, `bot.js:15271–17532`): Gold **komplett** auf Lila-Akzent (`#d4af37→#7c3aed`, `#f5d76e→#cc5de8`, `#8b6914→#6d28d9`, dazu `rgba(212,175,55,*)`/`rgba(245,215,110,*)` und die `--dgold*`-Tokens), Font auf `var(--font)`. **Bewusst dunkle Fläche beibehalten** — Dashboard ist dark-only designt; `background:var(--bg)` würde Light-Mode brechen. Alle Ersetzungen **scoped** auf den Block (andere Gold-Seiten unberührt).
- **Explore (`/explore`, `bot.js:17533–18942`):** Nur **Token-Hygiene** — 75 exakte `font-size`-Werte (12/13/15/16/20/28px) auf `--fs-*`-Tokens. **Gold bewusst behalten** (Shop/Glücksrad/Gewinnspiel/Tiers Bronze-Silber-Gold = gewollter Belohnungs-/Premium-Look — *nicht* entgolden). Geprüft: Duplikat-Selektoren `.foot/.rl-*/.tb` sind **keine Defekte** — legitime Varianten (Dark-Override via `@media(prefers-color-scheme:dark)`, `.rl-btn:disabled .rl-btn-icon`, Gewinn-Tier-Varianten). Nicht mergen. **~~Nebenbefund tipps/newsletter~~ (KORRIGIERT — war falsch):** Nachgeprüft — die `tipps`/`newsletter`-Tabs nutzen **bereits** das `[data-theme]`-Token-System (`var(--bg3/4)`, `--border/border2`, `--muted`, `--text`, 70+ Treffer); kein Light-Mode-Defekt. Die einzige `@media(prefers-color-scheme:dark)`-Stelle (`bot.js:18846`) ist die **standalone Legal-Page** (`_legalPage`, ohne `layout()`/JS → kein `[data-theme]` verfügbar, daher bewusst prefers-color-scheme). `#1e1e1e` im **Shop**-Tab ist ein gewollter dunkler Avatar-Kreis (weißer Buchstabe, beide Modi ok). → **Nichts zu tun.**
- **Profil + Einstellungen (`bot.js:19348–21246`):** Token-Hygiene — 74 exakte `font-size`-Werte auf `--fs-*`. `.pf-input`-„Duplikat" war False Positive (`.pf-input-with-icon .pf-input`). Kein Defekt.
- **Microcopy (Leerzustände):** Profil-Leerzustände mit Hinweis ergänzt (eigenes Profil motivierend, fremde Profile neutral „Hat noch nichts geteilt."/„…Projekte erstellt."), Newsletter-Leerzustand, konkreterer Notif-Fehlertext („Versuch es gleich nochmal." statt „Bitte später erneut versuchen."). **Begriffe bewusst unverändert** (Links/Posts/Projekte). Feed-Leerzustand war schon gut → nicht angefasst. Lade-/Filter-/CTA-/dynamische Leerzustände sind korrekt so → fertig.
- **Rest-Screens (`bot.js:13075–14460`) + Chat-Dateien:** `font-size`-Token-Hygiene für Nachrichten/Suche/Benachrichtigungen (30 Werte) sowie `chat-list-render.js`/`chat-detail-render.js` (15 Werte; gleicher Token-Scope, nutzen schon `var(--…)`). JS-berechnete `font-size:'+(size*0.38)+'` bewusst nicht angefasst. → `font-size`-Token-Hygiene damit **app-weit durch** (Rest sind Nicht-Skala-Größen 11/14/18px oder JS-berechnet).
- **CTA-Konsistenz:** Login-Link `→ Login` (`bot.js:5662`) auf `→ Zum Login` vereinheitlicht (an bestehende Variante). Sonst waren CTAs bereits konsistent; vermeintliche Treffer waren `<h1>`/`<title>`/Sub-Account (keine echten CTAs).
- **Spacing-Hygiene (app-weit):** ~785 **Einzelwert**-Deklarationen `padding/margin/gap` (+ Richtungs-Varianten) auf `--space-*`-Tokens (bot.js ~776, Chat-Dateien 9). Nur Einzelwerte (Trennzeichen-Constraint `[;}"']`), **keine Shorthands** → kein Token/Literal-Mix, wertgleich. Methode: temporäres `sed -E -f`-Skript (single-value-Regex), danach gelöscht. Negative Werte/`!important`/JS-berechnete bewusst ausgelassen.
- **Dashboard Light-Mode (vom User per Screenshot bestätigt ✅):** Dashboard ist jetzt **voll theme-aware** (vorher dark-only). Pass 1: `.dash-app`-Tokens auf kanonische (`--dink*→--bg*`, `--dline→--border`, `--dsub→--muted`, `background→var(--bg)`, `color→var(--text)`), plus scoped `color:#fff/#e7e7ea→var(--text)` und dunkle BG-Hex→`--bg*`. Pass 2 (nach Screenshots): „Geistertext" auf farbigen Flächen korrigiert — `.dash-topbar`-BG `rgba(8,8,10,.78)→var(--glass-bg)`, übersehenes Gold-Gradient `#f8e7a0/#a07a1c` in `.dash-btn-primary`+`.active`→`var(--accent)→var(--accent2)` mit `color:#fff`, sowie `.dash-row-avatar`/`.kbp-btn` zurück auf `#fff` (Text auf Gradient). **Regel gelernt:** `color:#fff` pauschal→`var(--text)` ist falsch für Text auf farbigen/Gradient-Flächen (Buttons/Tabs/Avatare) → dort Weiß behalten. **Offen (minimal):** 22× `rgba(255,255,255,…)` feine Trennlinien im Dashboard ggf. im Light-Mode unsichtbar — nur bei Bedarf theme-aware machen.
- **Dashboard-Modals theme-aware:** `.dash-modal-bg` hängt per `position:fixed` außerhalb `.dash-app` → die `--d*`-Tokens lösten dort nicht auf (Modals dunkel-auf-dunkel im Light-Mode). Fix: `--d*`-Token-Block **direkt auf `.dash-modal-bg`** definiert. Gilt für ALLE Dashboard-Modals (User-Detail, Event-Modals).
- **Dashboard aufgeräumt:** Buttons entfernt — Funnel Debug, Stats Debug, Kollab-Boost Preview, Neuen Sub erstellen, Play Store Listing. Handler-Funktionen/Seiten (`openFunnelDebug`, `/admin/play-listing` etc.) sind jetzt **toter Code** (Buttons weg = nicht erreichbar) — bei Bedarf komplett entfernen.
- **Feature: Account pausieren (soft)** — `bot-logic.js`: `pauseUserApi`/`unpauseUserApi` (setzen `u.paused`/`pausedAt`, kein Datenverlust). `bot.js`: `/api/admin/pause`-Route (Muster wie `/ban`), `isAppVisible` (`if(u.paused) return false`) + Feed-Stories-Filter blenden Pausierte aus Ranking/Explore/Suche/Stories aus; **Auto-Unpause** im Per-Request-Hook (`bot.js:5074`, try/catch-gesichert) sobald der User wieder aktiv ist. UI: „⏸️ Pausieren/▶️ Fortsetzen"-Button im User-Sheet bei Gefährliche Aktionen (`pauseUser()` Client-Handler). Admins können nicht pausiert werden.
- **Sheet-Aktionsbuttons (`.dash-act`) premiumiger:** flex-zentriert, `min-height:46px`, `radius:12px`, Press-Scale, Hover-Shadow, Danger-Hover-State.
- **MainBot-Sync-Statuskarte entfernt:** `#sync-health-banner` + `loadSyncHealth()`-Aufruf raus (Anzeige weg, Sync-Logik unberührt).
- **Toter Code entfernt (−328 Zeilen):** Client-Funktionen `openFunnelDebug/openStatsDebug/openKollabBoostPreview/testFunnelFire/loadSyncHealth/openSyncHealthDetail` + `.kbp-*`-CSS + Routen `/api/admin/funnel-debug|funnel-test|sync-health`. **`/api/admin/stats` BEHALTEN** (Haupt-Dashboard nutzt es, 2. Referenz in `loadStatsOverview`). `/admin/play-listing`-Seite bewusst gelassen (Routengrenze mehrdeutig, harmlos). **Lektion:** diese Funktionen liegen im Dashboard-Template-String → `node --check` prüft das innere JS NICHT; nur vollständige Funktionen (von `function X(){` bis schließendem `}`) entfernen.
- **Source-Vergleich entfernt** (`📱 Source-Vergleich`-Section + `renderSourceFunnel`-Aufruf). Funktion `renderSourceFunnel` bleibt als harmloser toter Code.
- **Dashboard-Sektionen ein-/ausklappbar:** idempotentes `makeSectionsCollapsible()` (nach `setInterval(refreshUsers…)`) hängt Chevron an jeden `.dash-section-hdr` + Klick toggelt `.collapsed` (CSS: `.dash-section.collapsed .dash-section-body{display:none}`). Idempotent gegen Re-Render (kein Doppel-Chevron).
- **Fix: gelöschte Diamant-Links** verschwinden jetzt auch aus der Dashboard-Liste — `diamondLinkAdminListApi` (bot-logic.js) filtert `deletedAt`. User-Feed (`diamondLinkFeedApi` → `_diamondActive`) filterte schon. Delete bleibt Soft-Delete (Daten erhalten), nur aus allen Ansichten ausgeblendet.
- **a11y: Dashboard-Modals tastaturbedienbar** (erster Hebel des app-weiten a11y-Passes): idempotentes `dashModalA11y()` (nach `makeSectionsCollapsible`) als **reiner Verhaltens-Layer** über alle `.dash-modal-bg` (User-Detail `~15784`, Event-Modal `~16040`) — **Escape** schließt das oberste Modal, **Backdrop-Klick** schließt, `role="dialog"`+`aria-modal="true"`+`tabindex="-1"` aufs `.dash-modal`, **Fokus** rein beim Öffnen (erstes fokussierbares Element) + zurück zum Auslöser beim Schließen (`_a11yTrigger`). MutationObserver erfasst dynamisch erzeugte/entfernte Modals (childList) → greift für alle Schließ-Pfade inkl. der `this.closest('.dash-modal-bg').remove()`-Buttons. **Modal-Erzeugung bleibt unangetastet.** (Hinweis: die abgestürzte Vor-Session hatte hier nur „11 Befehle/Datei gelöscht" — der Abbruch war ein Plattform-API-Fehler „thinking blocks cannot be modified", kein Code-Defekt; Arbeitsbaum war sauber, nichts ging verloren.)

- **Feature: Auto-Pause inaktiver User (>15 Tage).** Täglicher Sweep in `appCronTick` (Window 04:00-04:05, 1×/Tag via `einmalig`): nicht-Admin/nicht-Sub/nicht-gebannt, `lastActive = max(u.appLastSeen, getLastSeen(uid))` älter als 15 Tage → `pauseUserApi({ uid, reason:'15 Tage inaktiv', auto:true })` (soft, KEIN Datenverlust). `getLastSeen` (Session-`lastSeen`, persistiert) deckt allgemeine Aktivität ab, `appLastSeen` die App-Chat-Aktivität → Max ist robust. **Self-healing:** Auto-Unpause beim nächsten echten Request (`bot.js` ~5102) macht ggf. False-Positives sofort rückgängig. Schreiben nur unter `LOCAL_STORE`. `pauseUserApi` um `{reason, auto}` erweitert, `unpauseUserApi` räumt `pauseReason`/`autoPaused` mit auf. Admin-User-Sheet zeigt bei pausiert den Grund (auto vs. manuell) + „seit". `_adminIds` im Cron lokal aus `_d._adminIds` abgeleitet (nicht modulglobal).

- **Feature: Google Sign-In (OAuth 2.0), env-gated.** Buttons + Flow auf `/login`. Aktiv NUR wenn `GOOGLE_CLIENT_ID` **und** `GOOGLE_CLIENT_SECRET` (Env) gesetzt → `GOOGLE_OAUTH_ON`. Routen: `GET /auth/google` (CSRF-`state` in `_googleOAuthStates`-Map, TTL 10min → 302 zu `accounts.google.com`, `scope=openid email profile`, `prompt=select_account`, `redirect_uri` aus `x-forwarded-host`/`host`) und `GET /auth/google/callback` (state-Check → `_googleExchangeCode` am `oauth2.googleapis.com/token` → `id_token`-Payload via `_decodeJwtPayload`, ohne erneute Sig-Prüfung da direkt von Google über TLS → `email`/`email_verified` → `createEmailUserApi` find-or-create → Session minten + Cookie → neu: `/onboarding-instagram?first=1`, sonst `/feed`). Neue Felder am User: `googleId`, `signupSource:'google'`, `loginVia:'google'` in Session. Alters-/AGB-Zustimmung implizit bei Anmeldung (Timestamp gesetzt). **Prod-Redirect-URIs (in Google Console eintragen):** `https://creatorboostx.de/auth/google/callback` + `https://www.creatorboostx.de/auth/google/callback`. **Prod-Domain = `creatorboostx.de` (+ www).** Facebook/Instagram bewusst NICHT gebaut (Instagram-Login-API Ende 2024 abgeschaltet; Facebook bräuchte Meta-App-Review). Lokal getestet mit Dummy-Keys: 302/Button/CSRF ok.

- **a11y: App-Modals tastaturbedienbar (Fortsetzung des a11y-Passes).** Idempotenter Verhaltens-Layer `__cbModalA11y` **im globalen Shell-Script in `layout()`** (nach dem Img-Fehler-Handler, ~Zeile 3470) → greift app-weit (jede Seite via `layout()`, inkl. Chat & Dashboard). Selektor-basiert (NICHT heuristisch): `CBM_SEL = '.beta-modal-bg, #install-guide-modal, #thr-cust-modal, #thr-actions-modal'`. **Escape** schließt oberstes Modal, **Backdrop-Klick** schließt (idempotent — manche hatten's schon), `role="dialog"`+`aria-modal`+`tabindex=-1`, **Fokus** beim Öffnen auf ersten `button/[href]` (bewusst KEINE Text-Inputs → kein Keyboard-Popup auf Bottom-Sheets) + zurück zum Auslöser beim Schließen (`_cbTrigger`), MutationObserver (childList auf body, Modals werden direkt an body gehängt) für dyn. erzeugte/entfernte. **Modal-Erzeugung unangetastet.** Bewusst NICHT erfasst: Feature-Tour (`.ft-overlay`, eigene Steuerung) + Reaktions-Picker (`.chat-select-backdrop`, display-Toggle statt remove). **Wichtig gelernt:** `app-perf.js` wird NUR von den Chat-Render-Dateien eingebunden (chat-only), NICHT app-weit — app-weite Client-Logik gehört in den Shell-Script von `layout()`. Runtime verifiziert: `/feed` enthält den Layer, node --check innen+außen ok.

- **a11y: Screenreader-Namen für Icon-only-Controls.** Idempotenter Layer `__cbAriaLabels` im Shell-Script von `layout()` (direkt nach `__cbModalA11y`). Setzt `aria-label` NUR wo kein Name existiert (kein sichtbarer Text, kein `aria-label`/`-labelledby`) — abgeleitet aus `title`-Attribut, sonst aus bekannten Glyphen (`CBA_GLYPH`: ‹/←/< → „Zurück", ×/✕/✖ → „Schließen", ⋯/… → „Mehr", ☰ → „Menü"). **Kein Override, keine visuelle Änderung.** Läuft 1× bei `DOMContentLoaded`/`pageshow` über `button, a[href], [role=button]` (bewusst KEIN Mutation-Observer → kein Polling-Overhead). Glyph-Check ohne Regex (Template-Literal-Backslash-Falle vermieden). Logik unit-getestet, Layer in `/feed` verifiziert.

- **a11y: `:focus-visible` um `[role="button"]` ergänzt** (Zeile ~1852) — deckt die neu fokussierbaren Custom-Buttons (Modals/Dashboard-Header) mit dem globalen Fokus-Ring ab. Globaler Ring existierte schon für a/button/input/textarea/select/[tabindex].
- **Bewertung Rest-Roadmap (bewusst NICHT blind gegrindet — Anti-„zu weit gedacht"):**
  - **border-radius → Tokens: VERWORFEN.** `--radius`-Skala hat nur 3 Werte (xs6/sm10/16), real genutzt ~15 distinkte Radien (12px 146×, 14px 92×, 8px 80×…). Teil-Tokenisierung erzeugt Token/px-Mix = inkonsistenter als jetzt, null Nutzen.
  - **Loading-States vereinheitlichen: ÜBERSPRUNGEN.** Primitive (`.skeleton` 2359, `.sk` 1848, Spinner) existieren + werden genutzt. ~46 ad-hoc „Lädt…"-Spots alle umzubauen = viel Streuung, wenig Wert, Risiko.
  - **Light-Mode tipps/newsletter: ERLEDIGT/HINFÄLLIG** — nachgeprüft, Tabs sind schon theme-aware (s.o.), kein Defekt.
  - **spacing-Shorthands + Microcopy:** offen, aber brauchen Risiko-Abwägung bzw. Produkt-Stimme → nicht „blind" abarbeitbar.

- **Screenshot-Runde (Light-Mode QA vom User) — 2 echte Bugs gefixt:**
  - **Helper-Chat zeigte rohes HTML** (`<br>`, `<a>`) bei Standard-Antworten. Ursache: Server stuft client-gepostete `bot`-Rolle aus Sicherheit auf `user` (`bot.js:10293`, XSS-Schutz, bleibt!) → History-Render escaped `user`-Text (`12877`) → HTML als Klartext sichtbar. Fix: `cbHelperPersist` strippt bei `role==='bot'` das HTML zu lesbarem Klartext via `cbHelperStripHtml` (ohne Regex — Template-Literal-Falle). Live-Render bleibt HTML. Alt-persistierte Messages bleiben roh (regenerieren sich bei Nutzung).
  - **Explore-Tab-Kacheln „Geisterkarten" im Light-Mode** (`/explore`): `.explore-tab` (inaktiv) nutzte `background:rgba(255,255,255,0.025)` + `border:rgba(255,255,255,0.06)` (Dark-Mode-Design, weiß-auf-hell unsichtbar). Fix: `var(--bg3)`/`var(--border)` (+ hover `var(--bg4)`) → theme-aware, sichtbar in beiden Modi (wie die Tipps-Karten). Aktiv-State (Gradient) unberührt.
  - **Blasse Section-Eyebrow-Labels (Light-Mode) aufgehellt:** 5 Labels ("Aktuelle Highlights", "Links", "Profilbanner", "Profilring", "Meine Items") nutzten hartkodiert `color:rgba(255,255,255,.4)` (Dark-Design, im Light fast unsichtbar) → auf `var(--muted)` (theme-aware) umgestellt. Mission-FAB bleibt (User-Wunsch). Profil/Einstellungen/Nachrichten/Chat/Dashboard in Light+Dark geprüft = sauber.
  - **Bewusst gelassen:** Mission-FAB (`#cb-mission-fab`, `left:10px;top:24%`) — überlappt leicht den Feed-Rand, ist aber gewolltes Floating-Element; nur auf Wunsch verschieben. tipps/ranking/regeln/newsletter Light-Mode = ok bestätigt.

- **Chat-Performance (Branch-Thema) — DM-Render limitiert.** Problem: Die DM-Route (`bot.js:~13796`) lud ALLE Nachrichten eines Verlaufs und `chat-detail-render` rendert jede als Bubble (`msgs.forEach`) → riesiges HTML bei langen Chats. Verschärft durch den 3s-Poll (`~14075`), der bei neuen Nachrichten `location.reload()` macht → das ganze HTML wird jedes Mal neu gebaut. **Fix:** Route rendert nur die **letzten 60** (`CHAT_RENDER_LIMIT`), ältere via Link `?full=1` nachladbar (`_olderLink` oben im `#chat-msgs`). Macht Initial-Render UND Reload billig. Poll-Logik unberührt (`/api/messages/:uid` liefert weiter Gesamt-`count`, `chatKnownCount`=Gesamt → Neu-Erkennung korrekt). `chat-detail-render` NICHT angefasst. Unit-getestet (5/60/61/250/full), Chat-Seite lädt fehlerfrei. **Offen/optional (größer, später):** Poll von `location.reload()` auf inkrementelles Anhängen umstellen — mit dem 60-Limit aber nicht mehr dringend.

- **Stabilitäts-Audit Bot-Wegfall (telegram-bot gelöscht) — Ergebnis: sauber, keine Änderung nötig.** Geprüft: `postBot` = No-Op-Stub (`return null`); `fetchBotRaw` = `null` außer `/data`→lokaler Datastore; `fetchBot("/data")` liefert immer lokale Daten (nie null); alle übrigen `fetchBot`/`postBot`-Calls sind `LOCAL_STORE`-gezweigt oder fire-and-forget mit `.catch`, Ergebnisse via `|| {ok:false}` / `result && result.x` abgesichert. Einziger Direkt-`MAINBOT_URL`-Call (delete-link `~9991`) liegt im Nicht-LOCAL_STORE-`else` + `try` + `error`/`timeout`-Resolve → kein Hängen. Die `/api/diag`-Bot-Ping-Route ist null-sicher und meldet korrekt „Bot tot". → Kein Pfad hängt/spammt/dereferenziert null unsicher. Standalone-Migration robust.

- **Push-Notifications-Audit + 2 Verbesserungen.** Basis solide: VAPID mit FATAL-Guard, `Promise.allSettled`, Expired-Sub-Cleanup (410/404→delete) überall. `push-notify`/`push-broadcast` verlangen `x-bridge-secret` → **kein User-Spam möglich** (nur Server/Admin). **Fix 1 (Perf, echt):** `/api/post-link`-Broadcast an alle Subs war `await`-et → Poster wartete auf alle Push-Zustellungen. Jetzt **fire-and-forget** (`.then`-Cleanup), Poster kriegt sofort Antwort. **Fix 2 (defensiv):** `if(!session)`-Guard in `/api/push-subscribe` ergänzt (konsistent mit push-unsubscribe; Upstream-Auth-Gate redirected nicht-eingeloggte API-Calls aber ohnehin → 302, also harmlos/redundant). **Flag (Produkt-Entscheidung, NICHT geändert):** Beim Link-Posten geht ein Push an JEDEN User („🔥 Neuer Reel-Link!") — bei aktiver Community = potenzielle Notification-Müdigkeit. Throttling/Opt-out wäre sinnvoll, ist aber Engagement-Tradeoff → mit User klären.

- **Push gebündelt (Notification-Müdigkeit reduziert).** Vorher: pro Link-Post sofort ein Push an JEDEN User. Jetzt sammelt `_queueLinkPush` Posts in einem 10-Min-Fenster (`_LINK_PUSH_WINDOW_MS`, Fenster startet beim 1. ungesendeten Post), `_flushLinkPush` sendet EINE gebündelte Notification: 1 Post = Einzeltext wie bisher; 2 = beide Namen; 3+ = `N neue Reel-Links! A, B u.a. haben...`. Alle Poster der Bündelung ausgeschlossen (kein Self-Notify), fire-and-forget, Expired-Sub-Cleanup im `.then`. Buffer in-memory (Restart verwirft ein laufendes Fenster — unkritisch). Definiert nach `savePushSubs` (~542), genutzt in `/api/post-link`.

- **BUG (Push kam nie an) gefixt.** Der Push-aktivieren-Toggle in Einstellungen (`~20454`) holte den VAPID-Key von **`/push-vapid-key`** — diese Route existiert NICHT (nur `/api/vapid-public-key`). → 404-HTML → `atob()` warf → Subscription scheiterte IMMER. Zusätzlich fiel der Auto-Subscribe-Flow (`~3962`) bei fehlender Permission still aus (kein Prompt). Ergebnis: User bekam nie Pushes. **Fix 1:** Toggle nutzt jetzt `/api/vapid-public-key` (JSON `{key}`) wie der Auto-Flow. **Fix 2:** Auto-Flow subscribt nur noch wenn Permission schon `granted` ist (kein ungefragtes Prompt beim Laden; neue User opten via Einstellungen-Toggle). SW-`push`/`notificationclick`-Handler (`~5016`) waren korrekt. **Test-Tipp:** Einstellungen → Push-Toggle ein → Permission erlauben → sollte „Aktiv auf diesem Gerät" zeigen; dann Link posten (anderer Account) → gebündelter Push nach max. 10 Min.

- **BUG (Push-Deaktivieren hielt nicht) gefixt.** Toggle-Aus rief zwar `unsubscribe()` + `/api/push-unsubscribe`, aber der Auto-Subscribe-Flow beim nächsten Seitenladen re-subscribte sofort wieder (OS-Permission bleibt nach unsubscribe `granted`) → Toggle sprang zurück auf „An". Fix: persistenter Opt-out-Merker `localStorage['cb_push_optout']`. Gesetzt beim Toggle-Aus (`~20452`), gelöscht beim Toggle-Ein (`~20465`); Auto-Flow (`~3969`) bricht bei gesetztem Merker ab + räumt eine evtl. vorhandene Sub weg; `initPushStatus` (`~20476`) zeigt dann korrekt „Aus". → Deaktivieren bleibt jetzt aus.

- **DSGVO/Account-Löschung-Audit + Fix (Play-relevant).** `_deleteUser`/`deleteUser` (bot-logic) sind gründlich: entfernen User aus Following/Followers/Blocked, löschen Links/Posts/Likes/Kommentare/Messages/Notifications + User-Objekt, Sub-Accounts mit. Delete-Endpoint `/api/delete-my-account` (`bot.js:5378`) hat Session-Guard + löscht Session/Cookie. Export `/api/datenexport` (`5397`) hat Session-Guard bereits. **Fix (DSGVO-Lücke):** `pushSubs` (in bot.js, NICHT im Datastore) wurde bei Löschung nicht aufgeräumt → gelöschtes Konto hätte weiter Pushes bekommen + Push-Endpoint blieb gespeichert. Delete-Route entfernt jetzt alle `pushSubs`-Einträge des Users + `savePushSubs()`. **E2E verifiziert** (Reviewer-Login, da frischer User im Onboarding-Gate 302t): Push-Sub vor Löschung 1 → nach 0, Delete `{ok:true}`. **Hinweis (kein Fix):** zwei identische `deleteAccountDsgvo()` auf zwei Settings-Seiten — harmloses Duplikat, beide rufen korrekt `/api/delete-my-account`. App-Chat-Messages eines gelöschten Users bleiben (Community-Verlauf) — falls strenger gewünscht, separat anonymisieren.

- **Onboarding/First-Run-Audit — Ergebnis: sauber, keine Änderung nötig.** Flow E2E verifiziert (frischer Signup, LOCAL_STORE): Signup → `/onboarding-instagram?first=1` → Insta via `/api/update-profile` → `/onboarding-code` → Code via `/api/auth/set-my-code` (`setAppCodeApi`, Regex `^[a-z0-9_]{3,20}$`, Uniqueness-Check) → `/feed` (200). Gating prüft bei jedem GET den State neu (`!u.instagram`/`!appCodeChosenAt`) und redirected korrekt; kein Skip möglich, idempotent. Onboarding-Screens sind bewusst dark-only (gebrandet, wie Login). Kein Bug. (Hinweis: Onboarding ist NUR für Email-Signups; Reviewer-Bypass + bestehende User gehen direkt auf `/feed`.)

- **Bug-Hunt Kern-Ökonomie (Like/XP/Diamanten) — Ergebnis: robust, kein Exploit, keine Änderung.** Geprüft: `likeFromApp` (bot-logic ~402) ist idempotent (`wasLiked`→return, kein Doppel-XP → Offline-Queue-Doppelfeuer safe), Self-Like + Family-Self-Like (`getRootUid`) geblockt, XP nur bei neuem Like, Tag/Woche/Gesamt korrekt gestaffelt. `diamondLinkLike`/`prismaLinkLike`/`likeSuperlinkApi`: self/family/doppel/expiry alle abgesichert. `applyPostBonus` zeitfenster-gated (kein Doppel-Risiko). `appLikeCount % 100`-Diamant-Meilenstein zuverlässig (Zähler +1/Like, wird beim Merge nicht übertragen → konsistent). **Nebenbefund (kein Fix, kein aktiver Bug):** `addDiamond` (hot path) hat keinen Negativ-Clamp wie `addDiamonds` — im Like-Pfad ist amount immer positiv, daher harmlos; nur bei künftiger Wiederverwendung mit negativem amount beachten.

- **Landing-Page-Politur (`landing.html`, statisch ausgeliefert via `/`).** Prompt verlangte React/Tailwind+Pricing-Redesign — **bewusst NICHT** umgesetzt: Stack ist SSR-HTML (kein Framework-Rewrite), App ist kostenlos (keine Pricing-Tabelle). Landing war bereits premium (dark+gold/silver, Cormorant-Serif, Phone-Mockup, Live-`/api/community-stats`, FAQ-Accordion, Footer). **Gezielte Verbesserungen:** (1) **Hero-Copy** konkretisiert (vage „stärken sich gegenseitig" → „liken/kommentieren/teilen deine Reels … kein Bots"); (2) **Hero-Trust-Pill** (`#hero-trust`, grüner Puls-Dot) zeigt Live-Online (`onlineNow≥3`) bzw. Mitglieder (`≥10`), sonst versteckt — nährt sich aus bestehendem stats-Endpoint; (3) **FAQ +3** echte Fragen (IG-Richtlinien, Play Store/Browser, Daten/DSGVO-Löschung), `max-height` 300→420px; (4) **Footer** +Datenschutz/Impressum/AGB-Links (Routen existieren) + „Made in Luxembourg"; (5) **Conversion-Fix:** 3× interne `/signup`-CTAs hatten `target="_blank"` → riss Mobile/TWA-User in neuen Tab → entfernt. HTML tag-balanciert, Boot + Landing-Render verifiziert.

- **Security-Pass (Admin/Geld/IDOR/Rate-Limits) — Ergebnis: solide, 1 Härtung.** Geprüft: **alle** `/api/admin/*`-Routen sind auth-gegated (drei Helfer: `_adminAuthCheck` = Bridge-Secret|Session-Admin, `_isAdminRequest`, `_dashIsAdmin`; etwas fragmentiert aber lückenlos — keine offene Admin-Route). **IDOR:** Geld-/Aktions-Routen (pause/ban/reset/dm/suspend) alle hinter `_dashIsAdmin`; `switch-account` validiert `isOwnParent||isOwnSub` (kein fremder Switch); `pinned-redirect` nur whitelisted Insta-URL. Like/Diamant idempotent + self/family-geschützt (s. Ökonomie-Audit). **Rate-Limits:** globaler Sliding-Window 300/min/IP→429 (`bot.js:178/4903`) + dedizierte für Signup(5/h/IP)/Login(10/5min)/Magic-Link(30s)/PW-Reset(5min). **Härtung:** `/api/helper-ai` (Gemini = echtes Geld) hatte nur den globalen IP-Limiter → Per-User-Limit `_helperAiRate` 8/min/User (429) ergänzt, Cleanup im 5-min-Intervall. Logik unit-getestet. Kein weiterer Exploit gefunden.

- **Desktop/iPad Quick-Wins (additiv, mobile unberührt).** Bestand: ab 768px schon zentrierte App-Spalte (860px, Backdrop+Rahmen wie IG/X im Browser), Sheets via `inset:0`+`justify-content:center`+`max-width` automatisch zentriert, viele `:hover`-Regeln + Profil-Desktop-Tuning vorhanden. Ergänzt (nach dem 768px-Block, ~2252): (1) **breitere Spalte ab 1100px** (860→900px für `body`/`.bottom-nav`/`.cb-chatbar`); (2) **Hover-Feedback nur via `@media (hover:hover) and (pointer:fine)`** (cursor:pointer + nav/dm-row hover) → kein sticky-hover auf Touch; (3) **größere Touch-Targets auf Tablet** (`768px + pointer:coarse` → nav-item padding). Bewusst KEIN Voll-Breit-Strecken, KEIN Umbau bestehender Hover-Regeln (Risiko). Prompt-Phasen Hydration/Bundle/Folder-Refactor n/a (SSR-Monolith, kein Framework).

- **Backend-Robustheit-Scan — Ergebnis: solides Fundament, kein neuer kritischer Bug.** Verifiziert: Datastore schreibt **atomar** (temp + `renameSync`, `datastore.js:109`); **Crash-Guards** `uncaughtException`/`unhandledRejection` (`bot.js:14-18`); `parseBody` (`1711`) hat **Size-Limit** (`PARSE_BODY_MAX`, DoS-Schutz) und **resolved immer** (nie reject, Fallback form-encoded/`{}`) → die ~90 ungecatchten `await parseBody` sind risikofrei; Request-Body-`JSON.parse` (Uploads `8554/8608/8654/8681`) alle in try → 500 statt Crash; Boot-Datei-Loads in try; Client-`localStorage`-Parses mit `||'{}'`-Fallback. Zusammen mit Security-/IDOR-/Rate-Limit-/Ökonomie-/DSGVO-/a11y-/Push-/Chat-Perf-Audits dieser Session: **App ist verifiziert production-ready** (am Code geprüft, nicht behauptet).

- **Retention-Hebel #1–4 (ROI-Roadmap) umgesetzt.** (1) **Daily-Streak:** `touchStreakApi`/`getStreakApi` (bot-logic) — Felder `streakDays/streakLastDay/streakBest`; hochgezählt im Per-Request-Hook (`bot.js:5248`, 1×/Tag/User via `session._streakDay`, LOCAL_STORE); 🔥-Pill oben im Feed (vor Stories), zeigt Streak + Bestwert + „heute aktiv werden"-Hinweis. (2) **Win-back-Push:** im 04:00-Cron neuer `winbackInactive`-Sweep → bei 7 bzw. 12 Tagen inaktiv (vor Auto-Pause@15) je 1 persönlicher Push via neuem `pushToUid()`-Helfer; Flag `u._winbackStage` gegen Doppel. (3) **First-Win:** `postLinkFromApp` gibt `firstPost`+`posterName` zurück → `/api/post-link` sendet beim allerersten Post eines Neulings SOFORT Begrüßungs-Push an bis zu 25 aktive User (`getOnlineUids`), statt Bündelung → schnelle erste Reaktion = stärkster Retention-Moment. (4) **Near-Miss-Ranking:** Banner „Du bist #14 — nur 40 XP hinter #13" (nur wenn Diff ≤500), oben in der Gesamt-Rangliste. Neuer Helfer **`pushToUid(uid,title,body,url)`** (fire-and-forget, Expired-Cleanup) für personalisierte Pushes. Alles LOCAL_STORE-gated, Streak-Logik unit-getestet, Feed+Ranking-Render verifiziert.

- **Retention-Hebel #5–7 umgesetzt.** (5) **Missions-Reward sichtbar:** im Mission-FAB-Modal (`loadMissions`, bot.js ~12800) zwei Helfer `rew()` (Reward-Badge hinterm Label, +5 XP / +5 XP+💎) + `hint()` („Noch X Likes → +5 XP" unter Bar) für M1/M3 → klarer Anreiz + greifbare Nähe statt nackter Zahl. (6) **Level-up-Celebration:** server-gerenderte Rolle (`d.users[myUid].role`) im Feed; Inline-Script vergleicht mit `localStorage['cb_role_seen']` → bei Aufstieg einmalig `showBanner` (success, 6s) + Vibration. Kein State-Server nötig. (7) **Wochen-Recap:** in `wochenResetUndAuszahlung` (bot-logic, VOR `d.weeklyXP={}`) persönliche DM an in 7 Tagen Aktive (nicht Top-3/Admin/Sub/0-XP): „+X XP diese Woche · Wochen-Rang #N · 💎 aktuell" → Sinn/Stolz, Rückkehr am Wochenstart. Beide Dateien syntaxgeprüft, Feed+Mission-Render verifiziert. Damit ROI-Hebel #1–7 live.

- **Retention-Hebel #8 + #10.** (8) **Reziprozitäts-Nudge:** `likeFromApp` gibt `ownerUid`+`likerName` zurück → `/api/like` sendet via `pushToUid` einen Push an den Post-Owner („X hat deinen Reel engagiert — gib zurück"), gedrosselt 1×/Stunde/Owner (`_reciprocityRate`-Map, Cleanup im 5-min-Intervall), nicht bei Self/already-liked. Schließt die Gegenseitigkeits-Schleife = Community-Health. (10) **Empty-States:** Audit ergab — Feed („Noch keine Links heute" → „📸 Jetzt Link teilen"-Button via openPlusSheet), Posts, Chat haben bereits gute CTAs. Ergänzt: Superlinks-Empty bekam „⭐ Superlink posten"-Button. Streak von LOCAL_STORE entkoppelt (vorheriger Fix). ROI-Hebel #1–8 + #10 live.

- **Sub-Account-Limit 1 → 3.** `createSubaccountApi` (bot-logic) erlaubte normalen Usern nur 1 Sub (`subUid`-Check). Jetzt `MAX_SUBS=3`: zählt existierende Subs (`subUids`+Legacy-`subUid`, verwaiste ignoriert), blockt ab dem 4. mit klarer Meldung; Admins unbegrenzt. UI (`_allAccs`-Switcher) zeigte schon alle Subs + „➕ Neuer Sub"-Button immer → keine UI-Änderung nötig. Limit-Logik unit-getestet (3 ok, 4. blockt).
- **Prismalink entsperrt (war „COMING SOON").** Backend (`prismaLinkCreate`/`prismaLinkLike`, APIs `/api/prisma-link/*`) war bereits fertig — nur die UI an 4 Stellen gesperrt: (1) Plus-Sheet-Card disabled→aktiv (`openPrismaSheet`, „−100 💎"); (2) Feed-Tab-Placeholder→echter Skeleton-Root; (3) Tab-Nav `comingSoon:true`→entfernt (normaler Tab); (4) `initPrismaLinks`-Lade-Guard entfernt + `load()` rendert jetzt auch `tabEl` (voller Feed wie Diamond-Tab, lila Header). Helper-Text „(Coming Soon)" raus. Prisma-Sheet/`postPrismaLink` existierten schon. Verifiziert: Tab lädt (`prisma-tab-root`, kein „COMING SOON"), Feed-API 200.

- **Retention-Hebel #12 + #16.** (12) **Live-Aktiv-Pill** im Feed (über Stories, nach Streak-Pill): „● N Creator gerade aktiv — jetzt ist Engagement am stärksten" via `getOnlineUids()` (Admins rausgefiltert), nur ab ≥2 Online (sonst versteckt). Echtes Social-Proof. (16) **Fairness sichtbar:** dezenter Trust-Footer am Feed-Ende (nach `postsHtml`): „Echtes Engagement von echten Creatorn — keine Bots, keine gekauften Likes. Self-Likes & Fake-Engagement werden erkannt und sanktioniert." Macht die starke (unsichtbare) Anti-Cheat-Logik zum Vertrauens-Feature. Verifiziert: Feed rendert Footer, Live-Pill korrekt gegated. ROI-Hebel #1–8, #10, #12, #16 live.

- **Retention-Hebel #11 (Posting-Friktion).** Plus-Sheet-Link-Feld: "Einfügen"-Button (pastePlusLink → liest Clipboard, extrahiert Instagram-URL per Regex) + Live-Validierung (validatePlusLink via oninput → grüner Haken bei gültigem reel/p/tv-Link, Tipp bei Nicht-Reel, Warnung bei Nicht-IG). Senkt Hürde zum Posten = mehr Content. Verifiziert: rendert im Feed, Boot ok. **#20 Feedback-Kanal:** bereits vorhanden — Helper-Chat-Chip "💬 Direkter Support" leitet User-Fragen/Feedback an den Admin weiter (DM-Antwort). Kein neuer Code nötig. ROI-Hebel #1-8,#10,#11,#12,#16 live (+#20 vorhanden).

- **Feature: Referral-System (3 Schichten, voll E2E-getestet).** Belohnt langfristige AKTIVITÄT eingeladener Creator, nicht nur Registrierungen. **Kern (bot-logic):** `ensureReferralCode` (cb+6 Zeichen, `d.refCodeIndex` code→uid), `linkReferral` (dauerhaft `invitee.referredBy`, Selbst/Familie/Doppel-Schutz), `grantReferralMilestone` (idempotent via `invitee.refMilestones[key]`, gesperrte Einlader=kein Reward), `checkReferralProgress` (Likes 50/200, aktive Tage 7/15/30), `touchReferralActiveDay` (gesammelt mit Lücken, 1×/Tag), `referralStatsApi`, `communityBuilderBadge` (1/5/10/25 aktive→I/II/III/Elite, je mit `dailyDiamonds` 5/15/50/100), `communityBuilderRanking`. **7 Meilensteine:** Reg 50, FirstPost 30, 50L 30, 200L 50, 7d 50, 15d 100, 30d 250 💎. **Hooks:** Signup `body.ref`→linkReferral (Code aus ?ref= via localStorage cb_ref), Like→checkReferralProgress, FirstPost→grant, Login-Tag→touchReferralActiveDay (im Streak-Hook). **UI:** Seite `/einladen` (Link+Kopieren+Share, Stats Eingeladen/Aktiv/💎, Badge, Belohnungs-Tabelle, Community-Builder-Ranking), verlinkt aus Einstellungen. APIs `/api/referral/stats` + `/ranking`. „Aktiv"=hat Engagement gezeigt (firstPost OR active7). E2E: alle 7 Meilensteine 560💎 total, kein Doppel, Ranking/Badge korrekt; /einladen 16 Scripts node --check sauber.

- **Referral Anti-Missbrauch (3 Schutzschichten).** (1) **Signup-Reward 50→100💎, aber erst bei Insta-Username:** `linkReferral` vergibt signup NICHT mehr sofort; `updateProfileApi` triggert `grantReferralMilestone(uid,signup)` sobald ein Insta-Handle gesetzt wird → verhindert reine Anmelde-Farmen mit leeren Fakes. (2) **Clawback bei Bann:** `clawbackReferral(uid)` zieht alle für einen gebannten Eingeladenen gezahlten Referral-💎 vom Einlader ab (nie negativ, idempotent via `refClawedBack`), inkl. Sub-Accounts; eingehängt in `banUserApi`. (3) Bestehend: Selbst-/Familie-/Doppel-Schutz, Aktivitäts-statt-Registrierungs-Belohnung, gesperrte Einlader=kein Reward. Belohnungs-Tabelle nutzt jetzt `REFERRAL_MILESTONES`-Konstante (kein Duplikat). Einladen-Zugang prominent: Button im eigenen Profil + Settings-Karte. E2E: Signup-ohne-Insta=0💎, mit-Insta=100, Bann=Clawback auf 0, kein Doppel. KEIN IP-Limit (User-Entscheidung).

- **Referral-Verifizierung + Feed-Promo.** (1) Admin-Bestätigung vor Belohnung: Insta-Set eines Eingeladenen → `requestReferralVerification` (`d.referralPending`, Admin-DM). `grantReferralMilestone` zahlt nichts bevor `referralPending[uid].status===approved` (nachgelagerte Meilensteine warten mit). `approveReferral` (signup + Nachholung), `rejectReferral` (`refRejected` blockt dauerhaft). Dashboard-Sektion „Referral-Prüfungen" (Badge, Insta-Link, Bestätigen/Ablehnen, 60s-Refresh) + APIs `/api/admin/referral-pending|referral-decide`. (2) Clawback NUR bei manuellem Bann (`banUserApi`), nicht Auto-Bann. (3) Feed-Promo „Lade Creator ein" (ausblendbar `cb_ref_promo_hidden`, CTA→/einladen). E2E: wartet auf Approval, holt nach, Reject blockt; 24 Feed-Scripts node --check sauber.
- **DM-Politur app-weit (CreatorBoost-Nachrichten vereinheitlicht).** (1) **Zentraler Normalizer `_polishDM(text)`** (bot-logic, vor `sendCreatorBoostDM`): jede ausgehende DM läuft jetzt durch eine einzige Stelle → strippt Markdown-Reste (`**`, `*`, `` ` ``), entfernt Trennbalken-Zeilen (robuste Unicode-Range `─-╿` + Dash/`=~_-`), normalisiert Leerzeilen (`\n{3,}`→`\n\n`) und trailing spaces. Verdrahtet in `sendInAppDM` **und** `sendCreatorBoostDM` (Text + Notification-Preview); `dmUser` delegiert nur noch (Normalisierung zentral). **Bewusst KEIN Unterstrich-Strippen** — `_` kommt in Insta-Handles/Codes literal vor (vorher mangelte `dmUser` `@insta_user` → gefixt). (2) **Ranking-DMs mit Platz-Nummer:** Tages-/Wochen-Ranking zeigen jetzt „🥈 2. Platz im …" statt nur Medaille (vorher unklar); klarer Belohnungs-Block. (3) **Migration V2 (`_dmTidyV2`, eigener Flag):** hebt bereits gespeicherte alte Ranking-DMs im Verlauf auf den neuen Wortlaut (Platz-Nummer aus Medaille abgeleitet, doppeltes „Glückwunsch" entfernt). (4) **Guthaben-Zeile vereinheitlicht** auf Bullet-Stil `💎 Guthaben: X` (4 Stellen; vorher Mix aus `Guthaben: X 💎` / `… Diamanten`) → konsistent mit `⭐ +5 XP` / `💎 +2 Diamanten`. (5) **Link-Regeln-DM** kürzer/klarer (3-Schritte-Liste). Alles `node --check` + Unit-getestet (Normalizer: Markdown weg, Balken weg, Handles erhalten; Migration idempotent).
- **DM-Vollaudit (alle ~66 Stellen durchgegangen).** Ergebnis: Mehrzahl war bereits sauber/on-brand (Mindset-Stories, Verwarnungs-Eskalation, Support-Tickets, Kollabs, Referral, Bann/Sperre). Gezielte Konsistenz-Fixes: (a) **Rang-Aufstieg-DM** `⭐ X XP gesamt` → `⭐ Gesamt: X XP` (Format wie addXp/Missions); (b) **Event-Bonus** (Like + Post) Reward-Zeilen von `+5 💎`/`+10 XP` auf Bullet-Stil `💎 +5 Diamanten`/`⭐ +10 XP` inkl. Singular/Plural; (c) **Wochen-Gewinnspiel** Extra-Link-Emoji `🎁` → `🔗` (überall sonst genutzt). Haus-Standard dokumentiert: 1 führendes Emoji im Titel (kein „!"), Reward-Bullets `⭐ +X XP`/`💎 +X Diamant(en)`/`🔗 +X Extra-Link(s)`, Guthaben `💎 Guthaben: X`, Gesamt-XP `⭐ Gesamt: X XP`. Smoke-getestet (addDiamonds/removeDiamonds/addXp erzeugen korrekte DMs, Singular bei −1).
- **Fix: falsche M2/M3-Kommentarpflicht in alter Link-Regeln-DM.** Eine ältere Fassung der „✅ Dein Link ist gepostet"-DM behauptete „2-Wort-Kommentar = Pflicht (M2/M3 Missionen)" — **sachlich falsch**: die echten Regeln (`regeln-tab-content.js`, live) sagen korrekt: M1/M2/M3 unterscheiden sich nur durch die **Anzahl** gelikter Links (5/80%/alle), und der 2-Wort-Kommentar ist **Visit-before-Like-Pflicht bei JEDEM Insta-Like** (nicht M2/M3-spezifisch). Der Live-Code war schon korrekt; **Migration V2 erweitert:** ersetzt jede gespeicherte DM, die mit „✅ Dein Link ist gepostet" beginnt, durch den kanonischen `LINK_RULES_CANON`-Text (Link-Karte bleibt erhalten). Idempotent (Run 2/3 = 0), User-Nachrichten unberührt. **Hinweis:** Die Datei `regeln-tab.js` ist **toter Code** (bot.js lädt `regeln-tab-content.js`) und enthält Copy-Paste-Dubletten (M2/M3, W-M1/2/3, „2-Wort-Kommentar", Hard-Cap, Verwarnung je doppelt) — nicht gerendert, daher kein User-Impact; bei Gelegenheit löschen.

- **Fix: alte Link-Regeln-DM (falsche M2/M3-Kommentarpflicht).** Migration V2 ersetzt jede gespeicherte „✅ Dein Link ist gepostet"-DM durch kanonischen Text (Link-Karte bleibt). Alte Fassung behauptete „2-Wort-Kommentar = Pflicht (M2/M3)" — falsch: M1/M2/M3 unterscheiden sich nur durch Anzahl gelikter Links (5/80%/alle); 2-Wort-Kommentar ist Visit-before-Like-Pflicht bei JEDEM Like. Live-Regeln (`regeln-tab-content.js`) waren korrekt. Hinweis: `regeln-tab.js` ist toter Code (geladen wird `regeln-tab-content.js`).
- **Feature: Community-Activity-Ticker im Feed (Social Proof).** Rotierende einzeilige Pille oben im Feed (nach Live-Aktiv-Pill, vor Stories, `bot.js` ~11417) auf User-Wunsch. `getCommunityActivity(limit)` + `logActivity(type,name,detail)` (bot-logic, exportiert) aggregieren OHNE neues Tracking aus Ring-Puffer `d.communityActivity` (max 50, Dedup gleicher Event <60s), `dailyAwardsLog` (≤36h) + `weeklyAwardsLog` (≤4 Tage) Ranking-Sieger mit Medaille+Platz und „N neue Creator heute" (joinDate ≤24h, ohne Namen). Admins gefiltert, neueste zuerst, dedupe. Hooks: `logActivity('rank',…)` in `_badgeUpDM`, `logActivity('newmember',…)` beim ersten Post. Render: server-IIFE + JSON in `data-acts` (HTML-escaped), inline-`<script>`-Rotator wechselt alle 3,8s per `textContent` (XSS-safe). Nur ab ≥2 Events, graceful wenn Funktion fehlt. Verifiziert: beide `node --check` + `require()` grün, Aggregation 6 Items/0 Dups/deterministisch, Admin+stale gefiltert, Ring-Dedup ok. Echte Spitznamen (Ranking eh öffentlich) per User-Wunsch. **Lektion:** Nach wiederholten `git checkout`+Edits können Export-Liste und Funktions-Defs auseinanderlaufen (`node --check` bleibt grün, aber `require()` wirft ReferenceError) → immer mit `require()` final verifizieren.

- **Feature: Creator des Tages (Spotlight-Karte im Feed).** Tägliche Auszeichnung (keine Zufallsanzeige) für den aktivsten Creator des Vortags — auf User-Wunsch (Screenshot-Vorlage), gewählt: Auto nach Aktivität + große Karte oben + Mindesthürde. **Küren:** in `dailyRankingAbschluss` (bot-logic) wird `withScore[0]` (höchste Tages-XP + Aktivitäts-Score, Admins schon raus) als `d.creatorSpotlight` gespeichert — NUR bei Mindesthürde (`links≥1 ODER appLikeCount≥5`), nicht banned/Sub. Stats real: `streakDays/streakBest` (Tage aktiv), `links` (Beiträge), `appLikeCount` (Likes vergeben), `instagram`. **Getter `getCreatorSpotlight()` (exportiert):** liefert nur bei frischem `dayKey` (heute/gestern → keine veraltete Karte nach Inaktivität), nicht-banned; liest Name/Insta live aus `d.users`. **Render:** server-IIFE im Feed direkt nach dem Activity-Ticker, vor Stories (`bot.js` ~11436): große Karte mit Trophäe-Eyebrow, Avatar (Gradient gold→lila), Name+Insta, 3-Stat-Reihe (Tage aktiv/Beiträge/Likes, Singular/Plural korrekt), „Profil ansehen"-Button — 2× `/profil/<uid>`-Link. Alles `htmlEsc` (XSS-safe). Graceful '' wenn kein Spotlight/Getter fehlt. **Verifiziert E2E:** Küren wählt korrekten Gewinner mit echten Stats (Tom: 17 Tage/3 Beiträge/240 Likes), Mindesthürde greift (0 Posts+2 Likes → kein Spotlight), stale dayKey + banned → Getter null; Render-HTML tag-balanciert + escaping (`<script>`→`&lt;`); **realer Server-Boot + Reviewer-Login + `/feed` (200): Karte mit allen Elementen + 2 Profil-Links live im HTML.**

- **Feature: Admin-Link (Community-Push-Karte + Admin-Tab).** Admin-exklusive Spezialkarte, die bei jedem User ganz oben im Feed erscheint, bis er voll engagiert hat — dann verschwindet sie aus seinem Feed; +5💎 pro Engagement (einmalig je User), läuft 14 Tage. Gewählte Optionen: Bestätigungs-Button (wie Prisma) · nur Instagram-URL · sichtbar für alle außer dem Ersteller. Architektur eng an Prismalinks angelehnt. **Logik (`bot-logic.js`, nach Prisma-Block):** `d.adminLinks[id]={id,uid,url,message,createdAt,expiresAt,engagedBy[],engagedAt{}}`. Funktionen (alle exportiert): `adminLinkCreate` (nur `istAdminId`, Instagram-URL-Pflicht, message≤280), `adminLinkEngage` (nicht Ersteller, nicht doppelt → push engagedBy + `addDiamond(uid,5)` + DM), `adminLinkAdminDelete` (soft `deletedAt`), `adminLinkFeedCard(uid)` (ältester aktiver, NICHT erstellt/engagiert vom Caller — genau 1 Karte), `adminLinkListApi(uid)` (admin-only, alle aktiven + engagedCount/engagers). Konstanten `ADMIN_LINK_REWARD=5`, `ADMIN_LINK_LIFETIME_MS=14d`. **UI (`bot.js`):** (1) Platzhalter `<div id="admin-link-card-root">` ganz oben im Feed (über Spotlight, auf allen Tabs); Client-IIFE `initAdminLinkCard` lädt `/api/admin-link/card`, rendert gold/lila Karte (Badge, Message, „Auf Instagram öffnen"=Schritt1 setzt `window._alvisit`, „Engagiert +5💎"=Schritt2 mit 1,5s-Visit-Gate + confirm → `/api/admin-link/engage` → load() entfernt Karte). (2) Feed-Tab `{id:'adminlink',emoji:'🛡️',label:'Admin Link'}` nur via `if(_dashIsAdmin) _tabsMeta.push(...)`; Tab-Inhalt `#adminlink-tab-root` per Client-IIFE `initAdminLinkTab`: Erstell-Formular (URL+message+Button) + Liste aktiver Links mit Engagement-Statistik + Löschen. **Endpoints (`bot.js`, nach Prisma-API):** `/api/admin-link/card` (GET, eingeloggt), `/api/admin-link/engage` (POST, eingeloggt), `/api/admin-link/create` + `/api/admin-link/list` + `/api/admin/admin-link/delete` (alle `_dashIsAdmin`-Guard). Nur LOCAL_STORE-Pfad relevant (telegram-bot hat keine Prisma/Diamond-Routen → App läuft LOCAL_STORE=1); fetchBotRaw/postBot-Fallback trotzdem konsistent gesetzt. **Escaping-Konvention im Feed-`html(\`…\`)`-Template-Literal beachtet:** `\\'`→`\'`, `\\n`→`\n`, kein `${`/Backtick im Client-JS. **Verifiziert:** 14/14 Logik-Unit-Tests (Admin-Guard, +5💎 einmalig, Doppel-Engage ohne Extra, Ersteller-Ausschluss, Feed-Karten-Filter, Liste admin-only, Delete); realer Server-Boot + Reviewer(=Admin)-Login: alle 5 Endpoints (card/list/create/engage/delete) liefern korrekt, `/feed?tab=adminlink` 200, **emittiertes Client-JS via `node --check` valide** (Escaping ok), 🛡️-Tab + card-root + tab-root im HTML präsent.

- **Creator des Tages — Backfill (`bot-logic.js`).** Karte erschien nach Deploy nicht, weil `d.creatorSpotlight` erst beim nächtlichen `dailyRankingAbschluss` gesetzt wird. Neu: `_backfillCreatorSpotlightIfNeeded()` (vor `getCreatorSpotlight`, im Getter zu Beginn aufgerufen) kürt — falls keine frische Kürung (today/yest) existiert — den aktivsten Creator aus `d.gesternDailyXP` (sonst `d.dailyXP`) mit derselben Mindesthürde (`links≥1 || appLikeCount≥5`, kein Admin/banned/Sub), speichert mit `dayKey=gestern` + `backfilled:true`. Erscheint sofort, idempotent (überschreibt keine echte Kürung), wird heute Nacht regulär ersetzt. **Verifiziert:** 6 Unit-Tests (Backfill aus Gestern-XP, Hürde greift trotz höchster XP eines Nicht-Qualifizierten, dayKey=gestern, idempotent, frische Kürung bleibt, leer→null); realer Boot: Karte zeigt korrekten Gewinner (Tom 17/3/240) im `/feed`.
- **Admin-Link auch im „+"-Menü (`bot.js`).** User erwartete das Erstellen im Plus-Sheet (wie Prisma/Diamant), nicht nur im Feed-Tab. Neu: (1) admin-only `ps-card` „Admin-Link posten" (Schild-SVG, ADMIN-Badge) im Plus-Sheet via `${_isAdmin ? … : ''}` → `openAdminLinkSheet`; (2) eigenes `#adminlink-sheet` (URL + Aussage-Textarea, max 280, „Kostenlos · 14 Tage · +5💎") admin-only; (3) JS `openAdminLinkSheet`/`closeAdminLinkSheet`/`postAdminLink` (postet an `/api/admin-link/create`, dann → `/feed?tab=adminlink`). `_isAdmin` ist im Plus-Sheet-Template-Scope (berechnet ~2528, Template ab ~2541). Feed-Tab „🛡️ Admin Link" bleibt zusätzlich für Statistik/Verwaltung. **Verifiziert:** realer Boot+Admin-Login: „+" enthält die Karte, Sheet+Inputs+JS-Funktionen im HTML, ausgeliefertes `postAdminLink`-JS via `node --check` valide (confirm-`\n`-Escaping ok), Create-über-+ -Endpoint liefert ok + erscheint in Liste.

- **Admin-Link — Premium-Redesign + „Wer hat engagiert?" (`bot.js`/`bot-logic.js`).** Auf User-Wunsch (Screenshot): Karten schöner + Engagement-Sichtbarkeit. Neu: gemeinsame Top-Level-Helfer im Feed-Template `_alEsc`/`_alFmtRemaining`/`_alCss` (injiziert `#al-css`: animierter Conic-Gradient-Glow `.al-card-glow` gold→lila + Sheen, dunkler Premium-Body `.al-body`, analog Prisma) + `_alEngagers(id,engagers,count)` (Avatar-Stack + „❤️ Namen … engagiert" + „Wer? (N)"-Button → `showLikerModal('al-'+id)`, versteckte `#liker-rows-al-<id>`; Empty-State „sei die/der Erste"). **Feed-Karte** (`initAdminLinkCard`) nutzt jetzt `.al-card`+Glow, Gradient-Eyebrow, Autor-Zeile, Herz-SVG-Engage-Button, + Engager-Block (Social Proof). **Admin-Tab-Karte** (`initAdminLinkTab.renderLink`) ebenfalls Premium + großer Stat-Block (Engagements / 💎 verteilt) + Engager-Block + Löschen. `adminLinkFeedCard` liefert nun zusätzlich `engagedCount`+`engagers` (neueste zuerst, max 60). **Wichtiger Escaping-Bug gefunden+gefixt** durch Test des AUSGELIEFERTEN JS: im Avatar-Stack fehlte nach Ternary `(i?'-9px':'0')` das `+'` → String brach ab (`node --check` auf served HTML schlug fehl). Lehre: bei String-Concat-Ternaries im Feed-Template-Literal immer served-JS prüfen. **Verifiziert:** realer Boot + Admin-Login mit geseedetem Link (2 Engager): card-API liefert engagers, Feed+Tab rendern Glow/Stats/Wer-Button, `_alEngagers`-Output balanciert (Avatare/Namen/Modal/Empty), **served Client-JS via `node --check` valide**, Engage weiterhin +5💎.

- **Feed-Optimierung (Heute entstapeln) + Admin-Link-User-Vorschau + Profilbild (`bot.js`/`bot-logic.js`).** Auf User-Wunsch (Feed wirkte überladen, „wo ist Like-Button", „Profilbild fehlt"). Umgesetzt: **(1) Tab-Gating** — Community-Ticker, `#admin-link-card-root` und Creator-des-Tages werden NUR noch im Heute-Tab gerendert (`${tab==='heute' ? … : ''}`), andere Tabs (Älter/Diamond/Prisma/Admin) sofort sauber. **(2) Spotlight zwischen Posts** statt oben drauf: Creator-des-Tages aus dem globalen Header entfernt, als `_spotlightHtml`-Var nach dem 3. Post eingestreut (`heuteLinksRegular.map((x,i)=>renderLink(x)+(i===_spotInject?_spotlightHtml:''))`, `_spotInject=min(2,len-1)`; bei 0 Posts in den Empty-/Pinned-Zweig). **(3) Echtes Profilbild** im Spotlight-Avatar (`/appbild/<uid>/profilepic` Overlay über Initiale, onerror→remove). **(4) Admin-Link „So sehen es User"-Vorschau**: gemeinsame `_alUserCard(c,isPreview)` (Feed-Karte + Admin-Tab nutzen sie); `_alEngagers(key,…)` nimmt jetzt Modal-Key (Feed/Live=`al-<id>`, Vorschau=`alp-<id>`) → keine doppelten `liker-rows`-IDs. Admin-Tab: Management-Karte (Stats + Wer?-Modal) + Toggle „👁 So sehen es User" → lazy `_alUserCard(p,true)` aus `window._alLinks` (Engage-Button zeigt Vorschau-Alert statt zu engagieren, da Ersteller eigenen Link nicht engagieren kann). `adminLinkFeedCard`+`adminLinkListApi` liefern `engagedCount`/`engagers`/author.instagram/reward. **Verifiziert:** realer Boot + 4 Posts + Admin-Link(2 Engager): Heute zeigt Spotlight 1× zwischen Posts MIT Profilbild-`<img>`, Diamond-Tab 0× Spotlight + kein admin-root-DIV (Gating ok); served Client-JS `node --check` valide; `_alUserCard` Preview/Live korrekt (Vorschau-Tag+Alert vs echtes adminLinkEngageClick, getrennte Modal-Keys, balancierte Divs).

- **Admin-Link: Engagement-Pflicht prominent (`bot.js`).** Die winzige graue Zeile „Liken · Kommentieren · Teilen · Speichern …" (10.5px, .45 alpha) war kaum sichtbar (User-Feedback). Ersetzt durch auffällige Box in `_alUserCard` (greift auf Feed-Karte UND Vorschau): amber `border:2px #f59e0b` + Glow, Header „⚠️ Pflicht: Voll engagieren", 4 Icon-Chips (❤️ LIKEN · 💬 KOMMENTIEREN · 🔁 TEILEN · 🔖 SPEICHERN), Warnzeile (XP-Abzug/Reset/Bann). Platziert ZWISCHEN „Auf Instagram öffnen" (Schritt 1) und Engage-Button (Schritt 2), analog Prisma-Card. **Verifiziert:** Box+alle Chips+Warnung im gerenderten Card, alte graue Zeile weg, divs/spans balanciert, served Client-JS `node --check` valide.

- **Feed: Live-Leiste (entstapeln) + Liker-Modal-Fix (`bot.js`).** (1) Streak-Banner + Aktiv-Pill + Community-Ticker (3 gestapelte Header-Blöcke, teils auf allen Tabs) → EINE schlanke „Live-Leiste" nur im Heute-Tab: `🟢 N aktiv · 🔥 N Tage · <rotierender Community-Ticker>` (Trenner nur zwischen vorhandenen Teilen, leer→versteckt). Aktiv-Zahl ganz oben (User-Wunsch), Streak kompakt statt großem Banner, Ticker integriert. (2) **Liker-Modal-Bug**: `showLikerModal` macht für nicht-`dl-`/`pl-`/`cl-`-IDs einen Live-Fetch `/api/link-likers`, der für Admin-Link-Keys leer zurückkam und die gecachten Engager mit „Noch niemand geliked" überschrieb → `al-`/`alp-` zur Exclusion-Liste hinzugefügt. **Verifiziert:** Boot+Login: Live-Leiste mit 🔥-Streak-Chip im neuen Container, alte Streak-Banner+Aktiv-Pill-Texte weg, Liker-Fix im served JS, Seite wohlgeformt.
- **Login-Link-Domain gefixt (`bot.js`, Wahl b).** `creatorx.app` (IONOS-Parkseite) als hartcodierte Login-Link-Vorschau (`/onboarding-code`, Zeile ~6682) ersetzt durch dynamische `_codeBase`/`_codeHost` aus `x-forwarded-host` (Fallback AUTH_BASE_URL=creatorboostx.de) — wie `/einladen`. User-geteilte Login-Links zeigen jetzt auf die funktionierende Domain. Verifiziert: Boot+`/onboarding-code?preview=1` mit `x-forwarded-host: www.creatorboostx.de` → kein `creatorx.app` mehr, zeigt `www.creatorboostx.de/i/<code>`. (creatorx.app als Marke wäre alternativ via DNS-Anbindung bei IONOS möglich.)
- **HISTORIE (erledigt): Domain `creatorx.app` zeigte auf IONOS-Parkseite** („Domain vergeben"). App läuft auf `creatorboostx.de` (AUTH_BASE_URL, funktioniert); echte Invite-Links (`/einladen`) nutzen dynamisch die aktuelle Domain. ABER Code-Setup-Seite zeigt hartcodiert `https://creatorx.app/i/<code>` (bot.js:6682) als „Login-Link" → User teilen creatorx.app-Links, die ins Leere (IONOS) laufen. Fix-Optionen: (a) DNS bei IONOS — creatorx.app als Custom-Domain aufs Hosting (Railway/Northflank) zeigen lassen; (b) Code — Anzeige/Generierung des Login-Links auf die funktionierende `creatorboostx.de` umstellen. Branding-Entscheidung des Users → nicht eigenmächtig geändert.

- **Builder: Mission-1-Meilenstein zählt (+1) (`bot-logic.js`).** Eingeladene zählten bisher erst als „aktiv" fürs Community-Builder-Ranking bei `firstPost` ODER `active7` (hohe Hürde). Neu: Sobald ein eingeladener User erstmals **Mission 1** (5 Reels engagiert) schafft, bekommt der Einlader **+1 beim Builder**. Umsetzung: (1) neuer `REFERRAL_MILESTONES.m1` (15💎, label „Mission 1"); (2) `_referralInviteeIsActive` zählt nun auch `refMilestones.m1`; (3) in `checkMissionen` beim Flip `mission.m1=true` → `grantReferralMilestone(uid,'m1')`. Anti-Fraud bleibt: greift nur nach Admin-Freigabe der Einladung (`referralPending.approved`), idempotent. **Verifiziert:** 10/10 Unit-Tests (Grant nur für approved, +15💎 an Einlader, Builder-active +1, idempotent bei erneutem M1, unapproved blockiert, End-to-End via checkMissionen, Badge-Tier korrekt).

- **FIX: „Diamantlink veröffentlichen → passiert nichts" (`bot.js`).** User-Report: Button reagierte nicht, keine Fehlermeldung. Ursache: natives `confirm()` wird in der TWA/App-WebView teils verschluckt (gibt `false` zurück) → die Aktion brach VOR dem Server-Call lautlos ab. Kein Server-Limit (Diamantlink hat keine Tages-/Wochengrenze, nur 30💎). Fix: neues In-App-Modal **`cbConfirm(message)`** (Promise<bool>, echtes DOM, funktioniert überall) ersetzt `confirm()` in 8 kritischen async-Flows: postDiamondLink/postPrismaLink/postAdminLink (Posten) + diamondLikeClick/prismaLikeClick/adminLinkEngageClick + 2× pinnedEngageClick (Engagieren). `adminLinkEngageClick` zu `async` gemacht. **Escaping-Falle:** sed-Transform `!confirm(`→`!(await cbConfirm(` + `)) return`→`))) return` griff bei Zeile mit `))return` OHNE Leerzeichen (pinnedEngage) nicht → Klammer fehlte → manuell gefixt. Lehre: bei solchen Transforms beide Varianten (mit/ohne Space) prüfen. **Verifiziert:** server `node --check` + **ausgelieferter 43KB-`<script>`-Block via `node --check` valide** (alle `await cbConfirm` in async fns, Klammern balanciert), cbConfirm definiert, kein blockierendes confirm() mehr in Post-Flows. (Admin-Delete-/Account-Lösch-/Kollab-confirms bewusst NICHT umgestellt — destruktiv, false=safe.)

- **FIX: „Visit-vor-Like"-Gate brach Engagement in TWA (`bot.js`).** Folge-Report nach dem confirm-Fix. Diamant-/Prisma-/Admin-/Pinned-/Kollab-Boost-Engage prüften den Instagram-Besuch über **window-Globals** (`_dvisit_`/`_pvisit_`/`_alvisit`/`_cbvisit_`), die beim Zurückkehren aus Instagram in der TWA (Page-Reload) **verloren gehen** → man konnte nach echtem Besuch NIE liken; die Hinweis-`alert()` wurde zudem in der WebView verschluckt → wirkte „kaputt". Fix: neue Helfer `cbVisitTs(id)` (liest Besuchs-TS aus localStorage `cb_visited_links`, übersteht Reload — wie der reguläre Feed via `markLinkVisited`) + `cbToast(msg)` (sichtbarer In-App-Toast statt verschlucktem alert). Alle 6 Engage-Gates: `const vts = window[GLOBAL] || cbVisitTs(KEY)` + `cbToast(...)` statt alert; alle „Auf Instagram öffnen"-Links rufen zusätzlich `markLinkVisited(KEY)` (KEY-Präfixe: postId für dl/pl, `pin-`+uid, `cb-`+postId). **Verifiziert:** Boot+Login, served `<script>`-Block `node --check` valide, 0 verbleibende Visit-`alert()`, ≥5 Gates nutzen cbVisitTs, ≥6 markLinkVisited-Calls, ≥4 cbToast-Gate-Meldungen.
- **AUDIT offen (gemeldet, noch nicht umgebaut): native `confirm()`/`prompt()` in weiteren User-Flows** (z.B. Block-User, Kollab-Anfrage, Post/Sub/Projekt löschen, Report, Gmail-Eingabe `__betaSwitchToGmail`, Sub-Account-Name, Account-Löschung) + ~30 Admin-Dashboard-Stellen. Gleiches WebView-Risiko (confirm→false / prompt→null, lautloser Abbruch). Empfehlung: schrittweise auf `cbConfirm`/neues `cbPrompt` umstellen. Destruktive (Account-/Admin-Löschen) niedrigere Prio (false=safe).

- **FIX (komplett): ALLE nativen `confirm()`/`prompt()` → `cbConfirm`/`cbPrompt` (`bot.js`).** „mach alles" — flächendeckende Umstellung (47 confirm + 17 prompt) per string-/klammer-bewusstem Node-Transform (END→START, `matchParen` mit `'`/`"`/`` ` ``/`${}`/Escape-Handling), Wrap zu `(await cbConfirm(ARGS))` bzw. `(await cbPrompt(ARGS))`. Neuer `cbPrompt(message, default, opts)`-Helfer (Promise<string|null>, In-App-Modal mit Input/Textarea) neben `cbConfirm`. **Sonderfälle manuell:** (a) `onsubmit="return confirm(...)"` (logout-all-others) → non-blocking `cbConfirm(...).then(ok=>if(ok)f.submit());return false`; (b) `reportPinnedEngager`-prompt liegt in einem Source-String (escapte Quotes → matchParen-NO-MATCH) → manuell `=prompt(`→`=await cbPrompt(`; (c) Transform traf 3 `//`-Kommentare (`confirm()/prompt()`-Text) → revertiert. **5 nicht-async Funktionen async gemacht:** ipfAddSub, __betaChangeEmail, __betaSwitchToGmail, adminLinkDelete, acDoDelete (Rest war bereits async via await fetch). **Validierung:** source `node --check` ✓; Ganzdatei-Scan „await cb in nicht-async Fn" = 0 (echte); emittierte `<script>`-Blöcke mit cb-Changes auf feed/einstellungen/einladen/profil je `node --check` valide (ALL_CB_BLOCKS_VALID); Admin-Konvertierungen (16xxx–19xxx) im Source stichprobenhaft korrekt; 0 native confirm/prompt verbleibend (außer Kommentare + PWA-`deferredPrompt.prompt()`). Hinweis: vorbestehende Regex `/["\\]/g` (bot.js:2582, buildErrorHandler) ist NICHT von uns — node --check standalone meckert, im Browser unkritisch/ungenutzt.

- **Visit-Gate: Integrität nachgeschärft (`bot.js`).** Nach dem localStorage-Fix war das Gate ZU schwach (Besuch dauerhaft gültig → „einmal besucht = immer engagieren"; geräteweit geteilt → Sub erbt Besuch des Hauptaccounts). Zwei Korrekturen: (1) **Recency-Fenster**: alle 8 Engage-Gates verlangen Besuch zwischen 1,5 s und **30 Min** her (`(Date.now()-vts)<1500 || >1800000` → blockt). Alte/fehlende Besuche blockieren wieder, IG-Rückweg (Reload) übersteht es weiter. (2) **Pro-User-Namespacing**: `cb_visited_links` → `cb_visited_links_'+(window.MY_UID||'anon')` (6 Stellen: markLinkVisited/hasLinkVisited/cbVisitTs + early-Fallbacks + Like-Queue). Jeder Sub-/Hauptaccount hat eigene Besuchs-Historie → jeder muss Instagram selbst besuchen, bevor er liken/engagieren kann (gilt auch fürs reguläre Feed-Liken). Admin-`_alvisit` bleibt window-global (1 Karte/Feed); per-id-localStorage trägt nach Reload. **Verifiziert:** source node --check + served /feed: per-User-Key vorhanden, ≥4 Gates mit 30-Min-Cap, cbSetCount-Block weiterhin valide. (Diese Änderungen ohne Template-/Escaping-Sonderzeichen → kein Emitted-JS-Risiko.)
- **Audit-Ergebnis (gesamt):** `alert()` ist bereits app-weit zu Banner gepatcht (`window.alert`-Override, Z. 3615) → kein stilles-Fehler-Problem (außer Seiten ohne `#cb-banner`). 0 native confirm/prompt mehr. Block-für-Block `node --check` auf feed/einstellungen/einladen/profil: nur cbSetCount-Block war kaputt (gefixt) — sonst alles valide. PWA-`deferredPrompt.prompt()` korrekt unangetastet.

- **App-weiter Bug-Audit + Fixes (`bot.js`).** Systematische Suche nach den Fehlerklassen dieser Session. **Methode:** (1) jede Seite rendern → JEDEN emittierten `<script>`-Block `node --check` (fängt Syntax-Killer); (2) Source↔Served-Diff zum Finden **gestrippter Client-Regexe**. **Kern-Erkenntnis:** In Template-Literalen (`html(\`…\`)`) werden Regex-Backslashes verschluckt — `\d`→`d`, `\s`→`s`, `\.`→`.` (empirisch getestet). Jeder Client-Regex mit EINFACHEM Backslash im Template kommt kaputt im Browser an; korrekt ist DOPPELTER Backslash (`\\d` → emittiert `\d`). **Gefunden & gefixt (5 semantische Regex-Bugs):** Such-Tokenizer `q.split(/[\s,.!?;:]+/)` (splittete auf 's'!) + `kw.split(/\s+/)` (13691/13692), Initialen `safeName.split(/\s+/)` (19058), XP-Count-up `replace(/\D/g,'')` + `match(/[\d.]+/)` + `replace(/\./g,'')` (20288/20290) — alle `\` → `\\`. **Sweep-Ergebnis:** alle Script-Blöcke auf feed/explore/einstellungen/einladen/profil/diamanten/insights/benachrichtigungen/nachrichten/dashboard/ranking u.a. bestehen `node --check`; 0 verbleibende gestrippte Regexe (Source↔Served verifiziert); `new RegExp`-Strip: keine; native confirm/prompt: 0; alert() app-weit zu Banner gepatcht. **Tipp für künftige Client-Regexe im Template: IMMER doppelter Backslash** (Z. 3689 dokumentiert das bereits).

- **FIX: Builder/Referral — Sub-Accounts sind eigenständige Builder + retroaktive Meilensteine (`bot-logic.js`).** User-Report: „mit Sub ein Mitglied eingeladen, beim Sub steht es ist aktiv, aber Sub nicht im Ranking / kein Badge — soll NICHT auf Hauptaccount hochgehen, sondern beim Sub bleiben". Zwei Bugs: **(1) Sub-Accounts komplett aus Builder ausgeschlossen** — `communityBuilderRanking`/`payCommunityBuilderDaily` übersprangen `u.parent_uid`. Fix: `parent_uid`-Skip entfernt → jeder Account (auch Subs) ist ein **eigener** Builder mit eigenem Badge/Ranking/Tagesauszahlung; `referralStatsApi` bleibt PER-ACCOUNT (Sub-Einladungen rollen NICHT auf den Hauptaccount hoch). (Zwischenstand „auf Familie/Root aggregieren" war falsch verstanden und wurde zurückgenommen.) **(2) firstPost/m1 gingen vor Freigabe verloren** — `grantReferralMilestone` greift erst nach Admin-Freigabe (`referralPending.approved`); postet der Invitee VORHER, wurde firstPost verworfen und `approveReferral`→`checkReferralProgress` holte nur likes/active7 nach, NICHT firstPost/m1. Fix: `checkReferralProgress` vergibt jetzt retroaktiv `firstPost` (wenn `links>=1`) + `m1` (wenn `appLikeCount>=5`), idempotent. **Verifiziert:** Unit-Tests — Sub-Invite→Hauptaccount kriegt Badge+Ranking+Tagesauszahlung; Invitee postet vor Freigabe→nach Freigabe trotzdem Kredit; Anti-Fraud-Gate (ohne Freigabe kein Kredit) intakt; direkter Invite weiter ok. **Wichtig für User:** Referrals brauchen weiterhin **Admin-Freigabe** (Dashboard → Referral-Prüfung, `/api/admin/referral-decide`), bevor Badge/Ranking zählt.

- **Feature: Builder-Rang überall sichtbar + Rang-Aufstieg-Notification/DM + Ranking-Redesign (`bot.js`/`bot-logic.js`).** Auf User-Wunsch. **Logik:** `builderBadgeFor(uid)` (exportiert) + `_bldEmoji(uid)` + `_builderActiveCount(uid)` (pro Account); `_checkBuilderRankUp(uid)` in `grantReferralMilestone` → bei Tier-Anstieg `addNotification` (Glocke) + `sendInAppDM` („🏆 Rang aufgestiegen! … +X 💎/Tag"), einmalig je Stufe via `u.cbTier`. **Daten:** `builderEmoji` an alle Liker/Engager-/Author-Objekte in den Feed-APIs (diamond/prisma/admin/collab) angehängt. **Anzeige:** Hauptprofil-Hero (`_bldBadge` neben Name, via `myUid`), In-Feed-Profil (ipf nm-Zeile, via `uid`), Karten-Autor (Diamond/Prisma `aName` + Admin-Card), Liker-Modal-Zeilen (5 Stellen, `u.builderEmoji`). **Ranking-Redesign** (`/explore?tab=ranking`, `_cbRows`): Avatar mit Profilbild (`/appbild/uid/profilepic` + Initiale), tier-farbiges Badge-Pill (🌱grün/🤝amber/🏗️cyan/🏛️lila), Medaille/Rang-Nummer, Aktiv-Zahl in Tier-Farbe, „+X 💎/Tag", Top-3-Gradient-Highlight, „· du", klickbar zum Profil. **Verifiziert:** Unit-Test Rang-Aufstieg (Notif+DM+cbTier, kein Doppel); builderBadgeFor + builderEmoji in API; gerendert: Profil-Badge + Ranking-Elemente (Avatar/aktiv/💎-Tag) da; served JS aller Blöcke (explore/einstellungen) `node --check` valide.

- **FIX: Messenger zeigte ewig „ungelesen"/hohen Zähler trotz gelesener Nachrichten (`bot-logic.js`/`bot.js`).** User-Report. Ursache: **Bot-DMs** (Referral-/Rang-/Mission-Benachrichtigungen via die `sendInAppDM`-artige Funktion, Z.1613) wurden mit `to: uid` (NICHT String-gewrappt) erstellt → numerische `m.to` (numerische User-UIDs). `markMessagesRead` (1681) und der Badge-Zähler (`/api/messages-count`, 9190) verglichen **strikt** `m.to === uid`, die Chat-Liste (15043) aber **typ-tolerant** `String(m.to)===String(myUid)`. → Numerische `m.to` wurden in der Liste als ungelesen gezählt, aber beim Öffnen NIE als gelesen markiert → blieb für immer „ungelesen". Fix: (1) `to: String(uid)` bei DM-Erstellung; (2) `markMessagesRead`: `String(m.to)===String(uid)`; (3) Badge-Zähler: `String(m.to)===myUid` (konsistent mit Liste). **Verifiziert:** Unit-Test (2 ungelesen inkl. numerischer to → nach markRead 0); App-Chat-Unread ist separat & korrekt (timestamp-basiert via `appChatLastRead`, eigene Msgs/uid coerced); keine weiteren strikten `m.to/m.from`-Vergleiche übrig.

- **FIX: Admin-Antwort routet zum letzten Schreib-Thread des Users (`bot.js` `/api/send-message`).** User-Wunsch: Antwortet ein Admin einem normalen User, soll die Antwort dort landen, wo der User zuletzt geschrieben hat — persönlich (direkter Chat `[adminUid, userUid]`) ODER Support (`[creatorboost, userUid]`). Logik: pro Thread den Timestamp der letzten Nachricht VOM User suchen (`_lastFromUser`); ist der persönliche jünger → `sendMessageApi({from:adminUid,to})` (persönlich, from=Admin), sonst → `sendDmSingleApi` (als CreatorBoost). **Verifiziert:** In-Process-Unit-Test gegen echte botLogic-Funktionen (persönlich→landet im `[admin,user]`-Thread from=Admin, kein Leak in CreatorBoost-Thread; nur-Support→`[creatorboost,user]` from=creatorboost). Der frühere „daten.json-FAIL" war ein `pkill -9`-Flush-Artefakt (debounced Save nie persistiert), kein Code-Bug. Zusammen mit dem Merge-Fix (Admin SIEHT die persönlichen User-DMs) ist die Admin-Chat-Ansicht jetzt vollständig.

- **Feature: „Wer hat wen eingeladen"-Dashboard-Übersicht (`bot-logic.js`/`bot.js`).** User-Wunsch: überall sehen, wer wen eingeladen hat. Neu: `referralOverviewApi()` (bot-logic, exportiert) listet JEDEN Einlader (auch Sub-Accounts — eigenständige Builder, rollen NICHT auf den Hauptaccount hoch) mit allen Eingeladenen + Status (`approved`/`pending`/`rejected`/`linked` aus `referralPending` + `refRejected`) + aktiv-Flag (`_referralInviteeIsActive`) + Builder-Badge (`communityBuilderBadge`) + verdiente 💎 + Totals. API-Route `GET /api/admin/referral-overview` (`_dashIsAdmin`-Guard, LOCAL_STORE). Dashboard-Sektion „🌐 Wer hat wen eingeladen" (nach Referral-Prüfungen): klappbare `<details>`-Karten je Einlader (Builder-Emoji, Name, SUB-Badge, „N eingeladen · M aktiv · 💎X"), aufgeklappt je Eingeladener eine Zeile mit Aktiv-Punkt (grün/grau), Name (rot+🚫 bei Bann), Insta-Link, Status-Chip. `loadReferralOverview()` mit 60s-Refresh + Reload nach Bestätigen/Ablehnen. **Verifiziert:** Unit-Test (Anna 3 Invitees approved/pending/rejected+banned, Sub als eigener Builder), reale API mit geseedeten Daten (2 Einlader/3 Einladungen/2 aktiv, Sub korrekt eigenständig), **alle 15 ausgelieferten Dashboard-`<script>`-Blöcke `node --check` valide** (inkl. 112KB-Block mit `loadReferralOverview`; `<details>` statt onclick → kein Escaping-Risiko), Sektions-Skeleton im gerenderten `/dashboard` präsent.

- **🚨 KRITISCHER FIX: versehentliche Komplett-Account-Löschung statt Sub-Löschung (`bot.js`).** User-Report (Screenshot): „Oh nein ich hab gerade meinen account gelöscht, ich wollte nur den sub account löschen." **Root Cause:** Es gab GAR KEINEN Sub-Löschen-Button — `deleteSubAcc()` war definiert, aber NIE verdrahtet (`onclick` = 0×). Der Account-Switcher konnte nur wechseln + Sub erstellen. Wer seinen Sub löschen wollte, fand nur „🗑️ Account dauerhaft löschen" (= Hauptaccount + ALLE Subs weg, via `/api/delete-my-account` → `session.uid`) → Totalverlust. Zusätzlich war der Voll-Lösch-Confirm nur ein Ja/Nein-Tap (Doku versprach „LÖSCHEN tippen"). **Fix (3 Teile):** (1) **Sub-Löschen-Button** (🗑) pro Sub im Account-Switcher-Menü (neue `.ipf-switcher-row`/`.ipf-sub-del`-CSS, `event.stopPropagation()`), neuer Client-Handler `deleteSubAccById(uid,name)` mit klarer Meldung „NUR dieser Sub … Hauptaccount bleibt". (2) **`/api/delete-subaccount`** nimmt jetzt `sub_uid` aus dem Body (gezieltes Löschen EINES bestimmten Subs bei mehreren; Fallback Legacy `session.subUid`), Ownership via `deleteSubaccountApi` (`sub.parent_uid===session.uid`); Guard gegen Löschen des Hauptaccounts (`sub_uid===session.uid` → 400); überflüssige „erst zurückswitchen"-Sperre entfernt (`session.uid` IST immer der Parent → von überall sicher), `activeUid`/`subUid` der betroffenen Sessions werden zurückgesetzt. (3) **Voll-Löschen gehärtet:** Confirm sagt jetzt klar „HAUPTACCOUNT INKL. ALLER Sub-Accounts" + Hinweis „Nur EINEN Sub löschen? → Switcher → 🗑", und verlangt **„LÖSCHEN" tippen** (cbPrompt, trim+uppercase) statt nur Ja/Nein — beide `deleteAccountDsgvo()`-Kopien (account-Settings + Profil). **Verifiziert E2E:** realer Boot + Sub erstellt → gezieltes Löschen während man IM Sub ist → `{ok:true}`, **Hauptaccount bleibt erhalten**, Sub weg; Versuch Hauptaccount-UID als `sub_uid` → abgelehnt; Profil rendert 🗑-Button; alle 18 ausgelieferten Profil-`<script>`-Blöcke + 15 Shop-Blöcke `node --check` valide.

- **Feature: Premium-Shop „50 Diamanten Rahmen" (`bot.js`/`bot-logic.js`).** User-Wunsch (Bild) — premium Diamanten-Sinks. 10 edle Avatar-Rahmen à 50💎 (Fire/Ember/Lava/Gold Rush/Crystal/Ice/Snow/Rainbow/Prism/Bubble) als Erweiterung des bestehenden `activeRing`-Systems → rendern **überall** via `getRingBoxShadow` (mehrschichtige themen-farbige Glow-`box-shadow`, kein Render-Site-Change). Hinzu: `RING_ITEMS` (bot.js, mit `premium:true`) + synchron `ITEM_PRICES`/`ITEM_NAMES` (bot-logic.js, Kauf-Validierung läuft über `buyItemApi`). Shop-Tab (`/explore?tab=shop`): eigene Premium-Sektion mit gold/lila „💎 50 Diamanten Rahmen"-Banner (PREMIUM-Pill), `_ringCard`-Helper + `RING_ITEMS.filter(r=>r.premium)`; normale Rings bleiben unter „Profilring". **Verifiziert:** Kauf-Unit-Test (2 Rings gekauft, 120→20💎, zu-wenig-Balance abgelehnt), `node --check` + `require()`, Shop rendert Sektion + Rings, emittiertes Shop-JS valide.

- **Feature: Shop-Abteilungen (Storefront) + Builder-/Admin-Spezial-Rahmen in der „Tasche" (`bot.js`/`bot-logic.js`).** Folge-Wünsche (Bilder). **(1) Shop wie ein echtes Geschäft:** Storefront-Hero („🛍️ CreatorX Shop · Diamanten-Kaufhaus" + 💎-Balance-Pill) + horizontale Abteilungs-Chips (⚡ Boosts · 🎨 Banner · 💍 Rahmen · 💎 Premium) die per `scrollIntoView` zu den Sektionen springen (Anker `dept-boosts/banner/rings/premium` mit `scroll-margin-top`). **(2) Spezial-Rahmen (NICHT kaufbar, nicht im Shop):** 4 Builder-Rang-Rahmen `frame_builder_1..4` (🌱grün/🤝amber/🏗️cyan/🏛️lila, je Tier) + 1 `frame_admin` (🛡️ gold+blau, „wie im Bild") — als `special:true` in `RING_ITEMS` (Shop-Filter `!r.premium && !r.special` schließt sie aus), rendern überall via `getRingBoxShadow`. **(3) Tasche (Profil → Einstellungen → „Meine Tasche"):** neue Gruppe „✦ SPEZIAL-RAHMEN — freigeschaltet durch deinen Rang" zeigt die berechtigten Rahmen (Builder bis erreichtes Tier + Admin) zusätzlich zu gekauften Items, aktivierbar via `setRing`. **Entitlement (bot-logic):** `_frameEntitled(uid,ringId)` + Gate in `setActiveRingApi` — `frame_admin`→`istAdminId`, `frame_builder_N`→`builderBadgeFor(uid).tier>=N` (höherer Rang darf niedrigere tragen); normale Rings weiter `inventory`-gated. **Verifiziert E2E:** Entitlement-Unit-Test (admin↔frame_admin, tier1↔nur builder_1, tier3↔nicht builder_4, non-admin abgelehnt, deactivate ok); realer Boot+Reviewer(Admin,Tier1): Shop zeigt Storefront+4 Anker + KEIN frame_admin im Shop, Tasche zeigt SPEZIAL-RAHMEN mit frame_admin+frame_builder_1; `setRing frame_admin`→ok, `frame_builder_4`→„nicht freigeschaltet"; emittiertes JS (Shop 15 / Einstellungen 17 Blöcke) `node --check` valide. **Hinweis:** Rahmen sind mehrschichtige CSS-`box-shadow`-Glows (kein 3D-Render wie die Vorlage-Bilder — Krone/Text-Labels des Admin-Bildes sind in CSS bei 40px-Avataren nicht sinnvoll darstellbar; Gold+Blau-Glow nimmt die Bild-Farbwelt auf).

- **Feature: Papierkorb — gelöschte Accounts wiederherstellen (`bot-logic.js`/`bot.js`).** User-Frage „wie kann ich seinen Account wiederherstellen?" (realer Lösch-Unfall). Vorhandene Basis: `_deleteUser` legt vor jeder Löschung ein **vollständiges Backup des User-Objekts** in `d._deleteLog` ab (letzte 50, kein Zeitlimit — die „50 Tage"-Doku war ungenau). Es gab aber KEINEN Restore (die „Admin-Restore"-Doku war nie implementiert). Neu: `deletedUsersListApi()` (listet Backups deren uid nicht mehr aktiv ist, neueste zuerst, mit name/insta/email/xp/💎/SUB) + `restoreDeletedUserApi({uid})` (re-insertiert das Backup nach `d.users[uid]`, heilt parent/sub-Verknüpfung via `subUids`/`subUid`, entfernt den Log-Eintrag, lehnt Doppel-Restore + bereits-existierende ab). Admin-Routen `GET /api/admin/deleted-users` + `POST /api/admin/restore-user` (`_dashIsAdmin`, LOCAL_STORE, Cache-Invalidierung). Dashboard-Sektion „🗑 Papierkorb — gelöschte Accounts" (Badge, je Account Karte mit XP/💎/Email/Zeitpunkt + „♻️ Wiederherstellen", 60s-Refresh). **WICHTIG (Grenzen):** Backup enthält NUR das User-Objekt (Profil/XP/💎/Badges/Items/Email+`password_hash` → Login funktioniert wieder, da `authEmailPassword` `d.users` direkt scannt). NICHT wiederherstellbar (in `_purgeUidFromCollections` aus geteilten Collections entfernt, nicht gesichert): Posts/Links, vergebene Likes, Kommentare, Follow-Beziehungen, Chatverläufe. Restore nur möglich solange die Löschung unter den letzten 50 liegt. **Verifiziert E2E:** Unit-Test (Haupt+Sub gelöscht→beide im Papierkorb→Restore stellt xp/💎/email/parent_uid wieder her→Papierkorb leer→Doppel-Restore abgelehnt); reale Route (`/api/admin/restore-user` stellt User 887766 mit xp520/💎140 wieder her, `_deleteLog` danach leer); Dashboard rendert Sektion, alle 15 ausgelieferten `<script>`-Blöcke `node --check` valide (`\\n`-Escaping im Template beachtet). **Für den konkreten Fall:** nach Deploy → Dashboard → „🗑 Papierkorb" → User suchen → ♻️ Wiederherstellen (klappt nur, wenn das Backup noch in der Prod-`_deleteLog` liegt).

- **Politur: animierte Rahmen + schönerer Account-Switcher (`bot.js`).** User: „muss besser aussehen, Profilringe sehen nicht wie im Bild aus." (1) **Rahmen aufgewertet:** Premium-/Spezial-Rahmen tragen jetzt `r1`/`r2`/`rg` (Innen-/Außenring + Glow-Farbe); `getRingBoxShadow` rendert daraus einen **animierten „atmenden" Doppelring-Glow** via Inline-CSS-Vars + globalem `@keyframes cbRingAlive` (Glow pulsiert, kein Movement → reduced-motion via `@media reduce{[style*=cbRingAlive]{animation:none!important}}`, `!important` schlägt Inline). Funktioniert überall ohne Änderung an den Avatar-Render-Stellen. (2) **Switcher-Trash-Button** von klobigem pink-Emoji-Kästchen auf dezentes SVG-Trash-Icon (transparent, `opacity:.5`→rot bei Hover) umgestellt. **Verifiziert:** Boot+Reviewer (activeRing=pring_fire): `cbRingAlive` im Feed-Avatar + Keyframe im Stylesheet, Profil-Switcher mit SVG (kein Emoji), emittiertes JS (feed 23 / shop 15 / profil 18 Blöcke) `node --check` valide. **Ehrliche Grenze (dem User kommuniziert):** Die 3D-gerenderten Textur-Rahmen aus der Bildvorlage (echte Flammen/Kristalle) sind in reinem CSS nicht 1:1 darstellbar — dafür bräuchte es PNG-/Bild-Assets (overlay). Die animierten Glow-Rahmen sind die premium CSS-Annäherung.

- **Admin-Login-Hilfe + Fix: Ring-Vorschau zeigte nichts (`bot.js`).** Kontext: wiederhergestellter User (klar.erver) kam nicht rein („Passwort oder email falsch") — Account hatte kein/falsches Passwort. **(A) Login-Hilfe:** im Dashboard-User-Detail neue Sektion „🔑 Login-Hilfe" mit „🔑 Passwort setzen" (Route `POST /api/admin/set-user-password` → `setUserPasswordApi` via `localWriteNow`) + „🔗 Login-Link" (Route `GET /api/admin/user-loginlink` → baut `https://<host>/i/<appCode>`, der per Brute-Force-geschütztem Auto-Login OHNE Passwort einloggt; Fehler wenn kein appCode → dann Passwort setzen). E2E getestet: Passwort setzen → Login mit neuem Passwort `{ok:true}`; loginlink ohne appCode → klarer Hinweis. **(B) Bug gefixt:** Shop-/Tasche-Ring-Vorschau nutzte `box-shadow:${item.shadow}`, aber Premium/Spezial-Rahmen haben kein `.shadow` mehr (→ `undefined` → schwarzer Kreis ohne Ring, vom User per Screenshot gemeldet). Neuer Helfer `ringPreview(item,size,inner)` rendert ein **echtes farbiges Ring-Band** (Gradient-Kreis + Glow + dunkler Avatar-Kern) — in Shop (56px) + Tasche (50px). **Offen/ehrlich:** Foto­realistische 3D-Textur-Rahmen wie in der Bildvorlage gehen nur mit echten PNG-Assets (Overlay) — die Gradient-Ring-Bänder + animierten Glows sind die CSS-Annäherung. **Verifiziert:** Shop rendert Ring-Band (kein `box-shadow:undefined` mehr), Dashboard hat setUserPw/userLoginLink, emittiertes JS (shop 15 / dash 15 Blöcke) `node --check` valide.

- **🚨 FIX: Login scheiterte bei DOPPELTER Email (`bot-logic.js`/`bot.js`).** Realer Fall (klar.erver/Biggi): nach Wiederherstellung + zwischenzeitlicher Neu-Registrierung gab es **zwei Accounts mit derselben Email** (`birgit-bauer@hotmail.de`: @Klar.erverstand 5525009262 + @moment.malbitte 1780256633439). `authEmailPassword` nahm per `find()` nur den ERSTEN Treffer → „Passwort oder email falsch", obwohl ein passender Account existierte. **Fix:** `authEmailPassword` prüft jetzt ALLE Accounts mit der Email und loggt in den ein, dessen `password_hash` zum Passwort passt (sonst noPassword/falsch). **Admin-Werkzeuge zum Aufräumen von Duplikaten:** `POST /api/admin/delete-user` (→ `deleteUser`, Backup in `_deleteLog`, Admins geschützt) + Button „🗑 Account löschen" in Gefährliche Aktionen; `GET /api/admin/email-duplicates` (findet alle Email-Mehrfach-Accounts). **Verifiziert:** Unit-Test (Login mit originalpw→Original-Account, mit newpw→neuer Account, falsch→abgelehnt; Duplikat gelöscht→Email eindeutig, Original bleibt). **Empfehlung an User:** bei klar.erver das Duplikat @moment.malbitte löschen + auf @Klar.erverstand via „🔑 Passwort setzen" ein Passwort setzen → eindeutiger Login.

- **Feature: Titelschilder (kaufbare Banner unter dem Profil) + Fix Banner-Kauf + 8h-Event (`bot.js`/`bot-logic.js`).** Neue Kosmetik nach Ring-Muster: `TITLE_ITEMS` (8 Designs: PRO/SUPERSTAR/VIP/FEUER/EIS/LEGENDE/ELITE/ROYAL, je `bg`-Gradient + `tcol` Textfarbe + `glow`, 30–50💎), `titleBannerHtml(id)` rendert den Banner (`.cb-titleplate` mit Emoji + gestyltem Titel-Text) unter dem Namen im Profil. Kauf über vorhandenes `buyItemApi` (Preise/Namen in ITEM_PRICES/ITEM_NAMES ergänzt), `setActiveTitleApi`/`u.activeTitle` + Route `POST /api/set-active-title`. Shop: eigene „🏷️ Titelschilder"-Sektion + Abteilungs-Chip; Tasche (Einstellungen): Titel aktivieren/deaktivieren (`setTitle()` client, reload). **Wichtiger Nebenfix:** `/api/buy-item` ließ per Gate NUR `RING_ITEMS` durch → Titel UND **Banner** gaben „Unbekanntes Item" (Banner waren also nie kaufbar!). Gate erweitert auf RING_ITEMS+TITLE_ITEMS+BANNER_ITEMS. **8-Stunden-Option** im Admin-Event-Dauer-Dropdown (Dashboard) ergänzt (fehlte zwischen 6h und 12h, `28800000`ms). **Verifiziert:** Unit-Test (Titel kaufen→aktivieren→deaktivieren→nicht-besessen abgelehnt) + Live-Server (buy/activate ok:true, Profil-Banner rendert, Shop-Sektion da, 8h im Dashboard); JS shop/profil/dash je 0 invalid.

- **Titelschilder: Preise 50–500💎, Anzeige im Feed-Link + Royal-Krone (`bot.js`/`bot-logic.js`).** (1) **Preise** aller 8 Titel auf 50–500💎 gesetzt (PRO 50 / SUPERSTAR 100 / VIP 150 / FEUER 200 / EIS 200 / LEGENDE 300 / ELITE 400 / ROYAL 500) — synchron in `ITEM_PRICES` (bot-logic, Kaufpreis = Source of Truth) UND `TITLE_ITEMS.price` (bot.js, Shop-Anzeige). (2) **Feed-Anzeige:** aktives Titelschild des Posters erscheint jetzt in der „→ Öffnen"-Reihe **ganz links** (sowohl im normalen Link-Feed als auch bei Superlinks); neuer kompakter Renderer `titlePlateInlineHtml(id)` (kein Außenabstand, kleinere Schrift), Reihe auf `justify-content:space-between` umgestellt. (3) **Royal aufgewertet:** reicherer Lila→Gold-Gradient + **schräg sitzende Premium-Krone (SVG, ~20° gedreht)** über dem Banner via `royalCrownSvg(h)`/`royalCrownDeco(h)` (`crown:true` am Royal-Item; Emoji im Banner durch die Krone ersetzt). Krone erscheint überall: Profil-Banner, Shop-Karte, Tasche, Feed. **Verifiziert:** `node --check` beide Dateien, Live-Boot (home 200 / feed 302 redirect), Preise in ITEM_PRICES+TITLE_ITEMS konsistent 50–500. SW_VERSION → `v303-titlefeed`.

- **Titelschilder „sauber befüllt" + Royal-Krone auf der Ecke (`bot.js`).** User-Feedback (Screenshot): Label klebte links → große leere Farbfläche rechts; Krone schwebte statt zu sitzen. Fixes: (1) Schilder zentriert + einheitliche Breite — `.cb-titleplate` `justify-content:center` + `padding:6px 16px`; Profil-Banner `min-width:150px`; Shop-Preview `display:flex;justify-content:center;width:170px`; Tasche-Plate `width:128px;justify-content:center` → Label mittig in gefüllter Fläche, einheitliches Katalog-Bild. (2) `royalCrownDeco` neu positioniert: sitzt jetzt AUF der oberen rechten Ecke (Basis überlappt die Kante leicht statt zu schweben), `right:+h*0.10`, `top:-h*0.62`, `rotate(12deg)`, `transform-origin:bottom right`. SW_VERSION → `v307-platefill`. `node --check` ok.

- **🐛 FIX: Wochen-Mission „Alle Superlinks engagieren → +500 XP" zahlte nie aus (`bot-logic.js`/`bot.js`).** Community-Report (Sternenfuchs): „es gab diesmal nicht die 500 XP". Ursache: die Belohnung war **nie verdrahtet** — `wochenSuperlinkMissionGranted` wurde nur gelesen (`bot-logic.js:631`) + wöchentlich gepruned (`1394`), aber **nirgends gesetzt**; kein einziger `addXp(...500...)` für Superlinks existierte. Die Missions-Karte (`bot.js:13774`) + Hilfe (`685`/`7599`) warben „+500 XP" (Hilfe sogar „+2💎"), real lief nur ein separater **+1💎**-Engagement-Bonus (`bot-logic.js:4118`, Sonntags-Auslosung, andere/lockerere Eligibility: nicht wochengefiltert, schließt jeden SL-Poster aus). **Fix (User-Entscheidung: +500 XP nachrüsten, +1💎 bleibt; diese Woche rückwirkend):** (1) `grantWeeklySuperlinkMission(weekKey)` + Helper `_superlinkAlleGeliked` — Eligibility **1:1 wie die Missions-Karte** (≥1 fremder Superlink der Woche + Familie hat jeden geliked), nur Hauptaccounts (kein Admin/Sub), idempotent über das Flag, vergibt +500 XP via `addXp(reason:'superlink-mission')` (neues `_reasonLabel`). (2) Eingehängt in `runWochenGewinnspielApi` (Sonntags-Auswertung) → ab jetzt jede Woche. (3) **Einmaliger Backfill** in `appCronTick` (`d._slMissionBackfillV2`-Flag, läuft genau 1×) zahlt rückwirkend für **Vorwoche** (`getPrevBerlinWeekKey`, neu) UND diese Woche an bereits Berechtigte — der Montag-Reset (`wochenResetUndAuszahlung`) löscht `d.superlinks` NICHT, daher ist die Vorwoche (gestern Sonntag ausgewertet, = Sternenfuchs' eigentliche Beschwerde) noch auszahlbar. (4) Hilfe-Text `685` „+2💎"→„+1💎" (Realität). Exportiert. **Unit-getestet:** Anna (alle fremden SL geliked, eigener ausgeschlossen) → +500; Ben (1 SL verpasst) → 0; Sub/Admin → 0; 2. Lauf idempotent (0, kein Doppel); Vorwoche+aktuelle Woche getrennt korrekt. `node --check` beide Dateien ok. SW→`v308-superlinkxp`. **Hinweis:** Backfill greift nur für die Vorwoche (1 Woche zurück); ältere Wochen bewusst nicht (Daten/Fairness).

- **Titelschilder: Premium-Collectible-Redesign + 3 Bugs gefixt (`bot.js`).** User-Screenshots: Schilder „zu lang" (full-width gestreckt), „schwarzer Streifen am Ende" (Gradient-Dunkelkante + Layering-Artefakt durch Streckung), Krone schwebte. + Design-Brief „premium/AAA-Cosmetic". **Lösung:** zentraler Renderer `titlePlateHtml(t, size)` (size: banner|shop|tasche|inline) ersetzt 4 duplizierte Markup-Stellen (titleBannerHtml, titlePlateInlineHtml, Shop-Karte, Tasche). Layered Look: glossy Top-Sheen über `t.bg`, 2-töniger **Metallrahmen** via doppeltem `box-shadow 0 0 0 1.5px/3px` (neue `TITLE_FRAMES`-Map gold/silver/violet/bronze/frost + `frame`-Feld je Item), inset Licht/Schatten (Tiefe), **gravierter Text**, weicher Glow. **inline-flex → hugt Content** (nicht mehr zu lang); ausgewogene `bg`-Gradienten (keine Near-Black-Enden) → kein dunkler Streifen. `royalCrownDeco` neu (`right:6px;top:-h*0.80;rotate(10deg);origin bottom right`) → Krone SEATED auf der oberen rechten Ecke. Alte `.cb-titleplate*`-CSS entfernt (dead). **Verifiziert:** `node --check` ok; Standalone-Preview-HTML (gleicher Renderer) dem User zum Direktöffnen geschickt (kein Headless-Browser im Env → visuelle Abnahme durch User). SW→`v309-premiumbadges`. **Offen:** Feinjustierung nach Feedback; echte 3D-Textur-Materialien nur mit PNG-Assets.
- **Tasche als eigene immersive „Leder-Bag"-Seite (`bot.js`).** User: Tasche aus den Einstellungen ausgliedern + so gestalten, „dass man wirklich denkt man wäre in einer Tasche". Neue Route `/einstellungen/tasche`: Leder-Stage (radial-gradient Braun), Reißverschluss-Header (`.tasche-zip` + Pull-Tab), gesteppte Naht (dashed `::before`), recessed „Fächer" (`.tasche-pocket`, inset-shadow) pro Kategorie (Spezial-Rahmen/Profil-Rahmen/Titelschilder/Karten-Themes), Items als Pergament-Karten mit „Tragen/Ablegen". Selbst-enthaltene Leder/Gold-Palette (Hell-/Dunkelmodus identisch). Reuse ringPreview/titlePlateHtml/cardThemePreview + setRing/setTitle/setCardTheme (Page-Script, toast global). Empty→/explore?tab=shop. Inline-Tasche-Block aus `/einstellungen` entfernt → prominente Gold-Hub-Card oben. SW/APP→`v320-tasche`.
- **Karten-Themes Erweiterung: Vorschau-vor-Kauf + Legendär-Tier + CSS-Cache-Buster-Fix (`bot.js`/`bot-logic.js`).** (1) **Crash-Hotfix:** CSS-`<link>` war hart `?v=237` (immutable-Cache → Theme-CSS kam nie an); zuerst fälschlich an `${SW_VERSION}` gehängt → `ReferenceError` (SW_VERSION lebt nur im SW-Script-Text, nicht Node-Scope) → /feed 500. Fix: neue Node-Konstante `APP_VERSION` (oben), genutzt im CSS-`<link>` UND interpoliert in den SW-Script (`SW_VERSION='${APP_VERSION}'`) = eine Quelle, bricht beide Caches je Deploy. (2) **Vorschau vor Kauf:** „👁 Vorschau"-Button je Theme-Shop-Karte → Modal `#theme-preview-modal` mit echter Mock-`.post`-Karte im Theme + Kaufen-Button. Daten via `window.CB_SHOP_THEMES`, `previewTheme(id)`/`closeThemePreview()`, ESC schließt. (3) **Legendär-Tier:** 2 Themes (theme_legend_phoenix 250💎, theme_legend_prism 300💎, `legendary:true`). `cardThemeClass` hängt `cb-ct-legend` an; CSS: Glow-Puls `cbLegendPulse` (Theme-Farbe via `--ct-glowc`), Funkel `::after`/`cbLegendSpark` (`--ct-spark`), „✦ LEGENDÄR"-Badge `::before`. Shop-/Preview-Badges + ITEM_PRICES/ITEM_NAMES ergänzt. SW/APP→`v319-legendthemes`.
- **Karten-Themes: Komplett-Designs für Feed-/Post-Karten (`bot.js`/`bot-logic.js`).** Neues Shop-Cosmetic (Ersatz für die entfernten Ringe). 6 Themes (`CARD_THEME_ITEMS`): Obsidian Gold/Frost/Sunset/Rosé/Emerald/Aurora(animiert). **Mechanik:** ein Theme re-skinnt die ganze `.post`-Karte, indem `cardThemeClass(themeId)` die Klasse `cb-ct cb-ct-<sfx>` an die Karte hängt und `cardThemeCss()` (in den `const CSS`-Block injiziert, nach `.post{}`) pro Theme die **CSS-Variablen** (`--bg3/--bg4/--text/--muted/--muted2/--border/--border2/--accent`) + bg/border/box-shadow überschreibt → alle Kind-Elemente passen sich konsistent an, **identisch in Hell-/Dunkelmodus** (eigene Regel + `[data-theme=dark]`-Override, beide mit `.cb-ct-<sfx>` = höhere Spezifität als Basis-`.post`). Angewandt in `renderLink` + `renderSuperLink` via `poster.activeCardTheme`. Aurora: `cbCtAurora`-Keyframe (Neon-Glow-Zyklus), `prefers-reduced-motion` respektiert. **Spiegelt Titel-System:** Preise in `ITEM_PRICES` (80–150💎), Namen in `ITEM_NAMES`, `setActiveCardThemeApi` (Inventar-Check → `u.activeCardTheme`) + Export, Buy-Gate erweitert, API `/api/set-active-cardtheme`, Frontend `setCardTheme`, Shop-Abteilung `dept-themes` (+ Nav-Chip „🃏 Karten", `cardThemePreview`-Mini-Karte), Tasche-Subsektion „🃏 KARTEN-THEMES". `node --check` beide ok; Vorschau-HTML dem User geschickt. SW→`v317-cardthemes`.
- **Sonntag-20:00-Wochenjobs selbstheilend gemacht — Gewinnspiel + Mindset-Pick liefen manche Wochen nicht (`bot.js`/`bot-logic.js`).** User: „es wurde auch kein gepickt diese Woche". **Ursache:** Cron `appCronTick` (läuft 1×/Min) triggerte beide Sonntag-Jobs nur exakt bei `wochentag===0 && h===20 && m===0` — KEIN Catchup. Fiel der eine Tick in Minute 20:00 aus (Deploy/Neustart/Server-Recycle), war der Job für die Woche weg. **Fix:** (1) `runMindsetPickApi` pro Woche idempotent (`weeklyState.week===week` bricht ab, statt nur bei gesetztem pickedUid → schützt auch Skip/leere-Liste vor Catchup-Override). (2) `runWochenGewinnspielApi` pro Woche idempotent via neuem `d.wochenGewinnspiel.lastWeekKey`-Guard am Funktionsstart (war vorher NICHT idempotent → hätte bei Mehrfachlauf mehrfach Gewinner gezogen + Engagement-💎 doppelt vergeben). (3) Cron-Bedingung `h===20 && m===0` → `h>=20` (Catchup-Fenster Sonntag 20:00–23:59); In-Memory-Dedup via `_appCronSeen['sundayJobs_'+tagStr]` (1×/Prozess), echte Garantie = API-Idempotenz. Self-Healing: Prozess muss nur irgendwann Sonntagabend leben. Grenze: ganzer Sonntagabend down → Woche rollt Montag (Admin kann manuell „Picken"). `node --check` beide ok. SW→`v316-cronpickfix`.
- **6 Basis-Avatar-Ringe auf Premium-Collectible aufgewertet (`bot.js`, nur Daten).** Prompt „premium collectible avatar frames" (verlangte Flutter — App ist aber Web/CSS; User wählte „in echte App"). Die 6 Basis-Ringe (ring_flame/ocean/gold/purple/rainbow/diamond) waren simple `box-shadow`-Ränder; das **conic-Premium-System** (`.cb-ring-base` Material-Farbband + 3D-Bevel + `.cb-ring-deco` Facetten + wandernder Glanz-Sweep + `.cb-ring-gems` funkelnde Rauten + Glow + `cb-deco-flames/frost` + `cb-ring-inferno` Flammen-Spikes) war schon produktiv (Builder-Rahmen) und voll verdrahtet (ringFrameOverlay/ringPreview/getRingBoxShadow/_ringActive prüfen alle `item.conic`). **Upgrade = reine Daten:** jedem der 6 Ringe `conic`+`glow`(+`deco`/`gem`/`inferno`) als Material-Profil gegeben (Flame=inferno+flames, Ocean=frost+aqua-gems, Gold=geschmiedet+gems, Cosmic=violett+gems, Rainbow=Spektrum+gems, Diamond=frost+weiße gems). `getRingBoxShadow` gibt bei gesetztem `conic` autom. '' zurück → kein Doppel-Ring, keine Pipeline-Änderung nötig. `gradient`/`shadow` als harmlose Fallbacks belassen. Royal/Elite existieren bereits (pframe_gold PNG / frame_builder_4 conic). **Verifiziert:** `node --check` ok; Vorschau-HTML (alt vs neu, 120/80/40px) dem User geschickt. SW→`v312-premiumrings`.
- **Admin-Titelschild (einzigartig, nur für Admins) (`bot.js`/`bot-logic.js`).** User-Wunsch: eigenes Schild für Admin, Emblem statt Krone → **Gold-Wappenschild mit Stern** (`adminShieldSvg`/`adminEmblemDeco`, Pendant zu `royalCrownSvg`/`royalCrownDeco`, sitzt SEATED auf der oberen rechten Ecke wie die Krone). Neues Item `title_admin` (obsidian-bg, Gold-Frame, `emblem:'shield'`, `special:true`, KEIN `ITEM_PRICES`-Eintrag → `buyItemApi` lehnt Kauf ab). `titlePlateHtml` wählt Emblem per `t.emblem` (`shield`→adminEmblem, `crown`/`crown:true`→royalCrown). Shop filtert `!t.special` raus; Tasche zeigt `special`-Titel nur für Admins (`String(u.role).includes('Admin')`); `setActiveTitleApi` gated `title_admin` auf `istAdminId` (kein Inventar nötig). ROYAL bekam `emblem:'crown'` (zusätzlich zu `crown:true`, abwärtskompatibel). SW→`v310-adminbadge`. `node --check` beide ok.
- **FIX: Superlink-Mission zahlte Subs nicht + war Familien- statt pro-Account (`bot-logic.js`).** User: „mein Sub hat alles geliked, sollte DM bekommen, hat aber keine." Zwei Fehler: (1) `grantWeeklySuperlinkMission` schloss Subs aus (`u.parent_uid`-skip); (2) Eligibility (`_superlinkAlleGeliked` UND Missions-Karte `getWochenMissionen`) zählte FAMILIEN-Likes statt pro Account. Verifiziert: `sl.likes` speichert echte Account-UID (`likeSuperlinkApi` push `String(uid)`; Family-Self-Like via `getRootUid` geblockt). **Fix:** beide Stellen → **pro Account** (`s.likes.includes(String(uid))`) + `parent_uid`-skip entfernt → Subs verdienen EIGENSTÄNDIG (konsistent mit Builder/weeklyXP). Admin-Ausschluss bleibt. Backfill-Flag `_slMissionBackfillV2`→`V3` (V2 in Prod verbraucht → V3 läuft 1× mit korrigierter Logik nach; gesetzte granted-Flags bleiben idempotent). **Unit-getestet:** Admin-Main→0, Admin-Sub(alle)→500, Anna(alle)→600, Ben(1/2)→0, 2. Lauf idempotent. **Hinweis:** Auszahlung = IN-APP-Nachricht („✨ +500 XP"), KEIN Push. SW→`v311-slperaccount`.

- **🐛 FIX: Eingeladene zählten nicht fürs Builder-Ranking, wenn sie >7 Tage nach Beitritt (oder vor Admin-Freigabe) posteten (`bot-logic.js`).** User-Report: „eine bestätigte eingeladene Userin wurde aktiv + hat gepostet, Einlader ging trotzdem nicht ins Builder-Ranking." **Ursache:** der `firstPost`-Referral-Meilenstein (macht einen Eingeladenen „aktiv") wurde beim Posten NUR im `isFirstPostEver`-Zweig vergeben (`postLinkFromApp`, Z.~822) — der verlangt u.a. **Beitritt <7 Tage** (`NEW_MEMBER_MAX_AGE_MS`) UND `links===1`. Postete der Eingeladene erst >7 Tage nach Beitritt (oder vor der Admin-Freigabe, dann lehnt `grantReferralMilestone` mangels `approved` ab und der einmalige `isFirstPostEver`-Flag ist verbraucht), wurde `firstPost` nie gesetzt → `_referralInviteeIsActive` false → Einlader fehlte im Ranking. Heilung lief nur noch „lazy" bei nächstem Like (Z.705) oder neuem Aktiv-Tag — sonst nie. **Fix (2 Teile):** (1) `postLinkFromApp` ruft jetzt bei jedem Post eines Eingeladenen **unbedingt** `checkReferralProgress(uid)` (idempotent, freigabe-gegated) — unabhängig vom 7-Tage-Fenster und von der Reihenfolge Post↔Freigabe. (2) Einmaliger Backfill in der Cron (`d._refProgressBackfillV1`) ruft `checkReferralProgress` für ALLE `referredBy`-User → heilt bereits feststeckende Fälle nach Deploy. **Verifiziert (In-Process-Test gegen echte botLogic):** vor >30 Tagen beigetretene + bestätigte Userin mit 1 Post → vorher 0 Ranking-Einträge/kein Badge; nach `checkReferralProgress` → `firstPost` gesetzt, Einlader im Ranking mit 🌱 Builder I; Gegenprobe `pending` (nicht freigegeben) bleibt 0 (Anti-Fraud intakt). `node --check` + `require()` ok.

- **Builder-Rang-Label in Feed-Karten (`bot.js`).** User-Wunsch: neben dem Builder-Emoji in den Feed-Karten auch den Rang-Text. `_posterBld` in `renderLink` (~11635) UND `renderSuperLink` (~11830) zeigen jetzt `Emoji + Label` (z.B. „🌱 Builder I") statt nur Emoji — via `b.label.replace(/^Community /,'')` (server-seitig, identisches Muster wie der Profil-Hero `_bldBadge` ~22392; kein Template-Literal-Regex-Risiko). Pill bekam `gap:3px` + `padding:1px 7px`. Zeigt sich nur bei Postern mit Builder-Rang (aktuell z.B. Mindset.stories_ = 🌱 Builder I). `node --check` ok.
- **Bug bestätigt (Builder-Ranking): Eingeladene mit echtem Reel-Post wurde nicht „aktiv".** User-Screenshots: Sternenfuchs lud Sabine/@zitatxzeilen ein (bestätigt, 💎200 = 2× Signup), die postete im Feed einen echten Reel-Link (sichtbar als „Susanne @zitatxzeilen", 20:35) — trotzdem 0 aktiv, kein `firstPost`. Root Cause = das oben dokumentierte `isFirstPostEver`-Gate (firstPost-Grant beim Posten nur bei <7-Tage-Neumitglied/`links===1`; war hier false → Grant ausgelassen; Approval-`checkReferralProgress` lief vor dem Post mit `links=0`; ohne nachfolgenden Like nie erneut). → Behoben durch die zwei Fixes oben (unbedingter `checkReferralProgress` bei jedem Post + einmaliger Backfill `_refProgressBackfillV1`). Nach Deploy + erstem Cron-Tick wird Sabine aktiv → Sternenfuchs 🌱 Builder I.

- **Admin-Override „✓ aktiv setzen" + Backfill V2 mit Verknüpfungs-Reparatur (`bot-logic.js`/`bot.js`).** Sabine (@zitatxzeilen) blieb trotz Reel-Post + Freigabe „nicht aktiv" (Sternenfuchs nicht im Builder-Ranking). Da Prod-Daten von hier nicht einsehbar sind: deterministischer Admin-Knopf statt weiter raten. **(1) `referralForceActiveApi({inviteeUid})`** (exportiert): repariert fehlendes `referredBy` aus den `referrals`-Arrays (falls Übersicht zeigt, aber `referredBy` inkonsistent → `checkReferralProgress`/`grantReferralMilestone` stiegen still aus), setzt Prüfung auf `approved`, hebt `refRejected` auf, vergibt `signup`+`firstPost` (→ aktiv, +30💎 an Einlader, `_checkBuilderRankUp`). **(2) Route** `POST /api/admin/referral-force-active` (`_dashIsAdmin`, LOCAL_STORE). **(3) UI:** grüner Button „✓ aktiv setzen" je nicht-aktiver Invitee-Zeile in „🌐 Wer hat wen eingeladen" (`loadReferralOverview`) + Client-`referralForceActive(uid,btn)` (ohne `confirm()` — wird in mobiler TWA verschluckt). **(4) Backfill V1→V2** mit `referredBy`-Reparatur vor `checkReferralProgress` → automatischer Pfad heilt Linkage-Fälle nach Deploy ein weiteres Mal. **Verifiziert:** Force-Test (kaputtes `referredBy` → repariert + firstPost + Einlader 🌱 Builder I); `node --check` beide; **emittiertes Dashboard-JS** (Template-Literal-Escaping `\\'`→`\'`) via node --check valide.

- **Builder-Tagesbelohnung: 04:00 → 07:00 + DM + Notification + Push (`bot.js`/`bot-logic.js`).** User-Wunsch. (1) Cron-Hook aus dem `h===4`-Block in einen eigenen **`h===7 && m<=5`**-Block verschoben (07:00 Europe/Berlin; TZ ist in `bot.js:1` gesetzt). (2) `payCommunityBuilderDaily` schickt pro ausgezahltem Builder jetzt eine **Glocken-Notification** (`addNotification`) + **DM** (`sendInAppDM`, mit Rang + Betrag + aktuellem Guthaben) und gibt `paidList` zurück. (3) Cron-Hook sendet zusätzlich pro Builder einen **Push** (`pushToUid` → `/explore?tab=ranking`). Doppelzahlungsschutz (`cbDailyLastDay`) unverändert. **Verifiziert:** zahlt 5💎 (Builder I), `paidList` korrekt, DM/Notification ohne Fehler, 2. Lauf am selben Tag = 0 (kein Doppel); `node --check` beide.

- **Diamant-/Prisma-/Admin-Karten an normale Link-Karten angeglichen (`bot.js`).** User-Wunsch: Profilbild überall, Reel-Vorschau, Builder/Titel wie bei normalen Karten. **Gemeinsame Client-Helfer** (`window.cbCardEsc/cbCardAuthorRow/cbCardReel`, vor dem Diamond-Modul definiert): (1) `cbCardAuthorRow(p, accent)` = Avatar (`/appbild/uid/profilepic` + Initial-Fallback) + Name + Builder-Badge (Emoji+Label) + Rolle-Badge + Titelschild + @handle; (2) `cbCardReel(p, postId, visitVar)` = klickbare „Instagram Reel"-Vorschau (Banner-BG `/appbild/uid/banner` + Play-Button + Avatar/Name unten + Caption), setzt beim Klick die Visit-Variable + `markLinkVisited`. **Server-Anreicherung** `_decorateCardAuthor(a)` (vor Diamond-API): setzt `roleBadgeHtml` + `builderEmoji/builderLabel` (via `builderBadgeFor`) + `titlePlateHtml` (via `titlePlateInlineHtml` aus `u.activeTitle`); aufgerufen in diamond/prisma/admin-card-API. **Wiring:** diamond/prisma renderCard + `_alUserCard` ersetzen die alte Name-Zeile durch `cbCardAuthorRow` und fügen `cbCardReel` ein (Visit-Vars `_dvisit_<id>`/`_pvisit_<id>`/`_alvisit`); „Auf Instagram öffnen"-Button + Engagement-Gate bleiben. **Verifiziert:** `node --check` + **emittierte Helfer** (Template-`\\'`→`\'`) syntaktisch valide + Runtime-Smoke (cbCardAuthorRow/cbCardReel laufen, onclick korrekt escaped). **Offen (minor):** Admin-Tab-Vorschau (`adminLinkListApi`-Daten) ist nicht serverseitig dekoriert → zeigt Avatar+Name+Builder-Emoji, aber evtl. ohne Rolle/Titel.

- **Feature: Verwarnungs-Hinweis beim nächsten Login (`bot.js`/`bot-logic.js`).** User-Wunsch: verwarnte User sollen beim nächsten Online-Kommen ein Modal sehen („Achtung — du wurdest verwarnt! Du hast noch X/5 Chancen … schau dir diese Anleitung an" + Öffnen-Button zu einem Instagram-Link). **Umsetzung:** (1) `u.warnNoticePending=true` an allen 3 Verwarnungs-Stellen (`addWarn`, `applyWarningEscalation`, Melde-Warn in `reportDecideApi`). (2) Feed-Route rendert (nach dem Level-up-Block, vor der Live-Leiste) **einmalig** ein zentriertes Modal, wenn `warnNoticePending` (nicht bei banned): Rest-Chancen `5−warnings`, „Verwarnung X/5", + „Anleitung ansehen →"-Button (`d.warnGuideUrl`), + „Verstanden". Flag wird beim Rendern gelöscht (consume-once, `datastore.saveDebounced`); Schließen via `this.parentNode.parentNode.remove()` (keine Quote-Escaping-Fallen). (3) Konfigurierbarer Link `d.warnGuideUrl` via Admin-Route `GET/POST /api/admin/warn-guide` (https-Normalisierung) + Dashboard-Section „⚠️ Verwarnungs-Anleitung" (Input + Speichern, `loadWarnGuide`/`saveWarnGuide`). Leerer Link → kein Button. **Verifiziert:** `node --check` beide + emittiertes Dashboard-JS valide. Standalone-Vorschau-HTML dem User geschickt.

## 8. Nächste sinnvolle Schritte

0. **a11y-Pass app-weit fortsetzen** (Dashboard-Modals sind erledigt): weitere `<div onclick>` → echte `<button>`/`role`, fehlende `aria-label` an Icon-Buttons, restliche Modals (App-Chat/DM/Helper) mit `role="dialog"` + Escape/Backdrop nach dem `dashModalA11y()`-Muster. Gezielt pro Screen statt blind app-weit (viele Dateien).
1. ~~tipps/newsletter Light-Mode~~ — **hinfällig** (nachgeprüft: schon theme-aware, kein Defekt; siehe Log).
2. Restliche Spacing-Shorthands (`padding:12px 16px` etc.) tokenisieren — nur sinnvoll mit per-Wert-Tokenisierung, mehr Aufwand/Risiko.
3. Onboarding-Flow-Texte / weitere Microcopy auf Wunsch.

## 9. Hinweise / Fallen

- **Gold-Premium-Seiten** (`bot.js:5862, 6216, 6302` u. a.) sind **weiterhin gold** — das ist gewollt, nicht anfassen ohne Auftrag.
- Scoped-Ersetzungen in `bot.js` per `sed -i 'START,END s/…/…/g'` (Zeilenbereich!), nie global — sonst werden andere Seiten getroffen.
- Vor Hex→`var()`: Kontext prüfen (CSS vs. SVG-`fill=`/JS-String). In JS-Strings nur Hex→Hex ersetzen.
- **Community-Builder-Ränge + Tagesbelohnung (User-Wunsch).** Schwellen geändert auf **1/5/10/25 aktive** Einladungen → 🌱 I / 🤝 II / 🏗️ III / 🏛️ Elite. Jeder Rang hat ein `dailyDiamonds`-Feld (5/15/50/100) und zahlt **täglich** aus: `payCommunityBuilderDaily(dayKey)` in bot-logic (iteriert User, berechnet aktive Einladungen, zahlt rangabhängige 💎 1×/Tag via `cbDailyLastDay`-Flag gegen Doppelzahlung, `cbDailyTotal` akkumuliert; skippt Admin/banned/paused/Sub). Cron-Hook in `appCronTick` (Fenster h===4, `einmalig('cbDaily_'+tagStr)`, `LOCAL_STORE`-gated, in `localWrite`). UI-Refs aktualisiert: `_cbBadgeShort` (Ranking-Tab, 25/10/5/1), regeln-tab.js r-einladen Badge-Rows (+ „💎/Tag" perks), /einladen Badge-Banner zeigt `+X 💎 pro Tag` + neue „🏅 Community Builder Ränge"-Tabelle (aus `communityBuilderBadge`, keine Drift). E2E: tier II=15/tier I=5 korrekt, banned/paused/admin/0-aktiv geskippt, kein Doppel am selben Tag, zahlt am Folgetag erneut; alle 3 Dateien node --check sauber. **Premium-Look (Folgewunsch):** Rang-Emojis sitzen jetzt in metallischen Medaillen (Farbring je Tier: Smaragd→Silber→Gold→Platin/Violett) via `_cbMedal`/`_tierRing`-Helper auf /einladen (Badge-Banner + Ränge-Tabelle) + `.cb-medal`-CSS in regeln-tab.js. Ranking-Tab-Builder-Box verlinkt auf `/explore?tab=regeln#r-einladen` („Regeln & Ränge"; Hash = volle Section-ID `r-einladen`).
- **Git-Workflow (User-Wunsch, dauerhaft): IMMER auf `main` pushen.** Nach jedem Commit auf dem Arbeits-Branch direkt als **Fast-Forward** auf `main` (`git push origin <feature>:main`) — ohne Rückfrage, keine PRs. `main` = Produktion (Deploy via Procfile/Dockerfile). **Nur FF, nie `--force`;** echtes Remote-`main` per `git ls-remote origin main` prüfen, lokaler `main`-Ref kann stale sein. Wenn nicht FF-bar: erst rebasen, nicht forcen.
